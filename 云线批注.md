# CAD Auto CloudNote 软件开发白皮书

**版本：** v0.1.0（审核修复版）  
**技术栈：** C# / AutoCAD .NET API  
**目标平台：**  
- AutoCAD 2007-2012（远古兼容宿主，net35 / WinForms 双轨降级）
- AutoCAD 2014-2020（经典兼容宿主，net45 / WPF）  
- AutoCAD 2021-2024（主流基线宿主，net48 / WPF）  
- AutoCAD 2025-2026（新一代扩展宿主，.NET 8.0 / WPF）  
- AutoCAD 2027（未来世代宿主，.NET 10.0 / WPF）
- 中望、浩辰 CAD 2021+（适配验证与最小可用适配层）  

> 说明：v0.1.0 建立“多版本分发架构（Shared Project + 独立宿主壳 + 双轨 UI）”完整覆盖 AutoCAD 2007-2027。针对 AutoCAD 2007-2012 建立独立的 Net35 兼容宿主外壳（具备 WinForms 双轨降级支持与 Inno Setup 注册表自动注入加载），针对 AutoCAD 2014-2020 建立 Net45 宿主，针对 AutoCAD 2021-2024 建立 Net48 基线宿主，针对 AutoCAD 2025-2026 建立 Net8 宿主，针对 AutoCAD 2027 建立 Net10 宿主，各宿主严格分包隔离与系列界定，严禁用单个程序集宣称跨 CLR 覆盖全系。

**文档日期：** 2026年9月

---

## 目录

1. 项目概述  
2. 技术架构  
3. 数据模型  
4. 核心功能模块  
5. 数据一致性保障  
6. 技术要点与最佳实践  
7. 版本兼容性  
8. 开发计划  
9. 部署方案  
10. 版本规划  
11. 附录：修复日志、关闭项与编码规范  

---

## 1. 项目概述

### 1.1 产品定位

CAD Auto CloudNote（CloudNote）是一款兼容 AutoCAD 及国产 CAD 平台，解决原生 CAD 云线功能薄弱、天正批注封闭、探索者批注偏重协同流程的问题，提供**数据开放、部署轻量、体验专业**的批注解决方案。

v0.1.0 吸收天正 T30 与探索者易批注的核心能力，形成完整单机审图工作流，并实现版本对比联动、非模态管理面板、状态可视化、AI 语义审查、审计日志、审查包、批量多图合并、天正比例深度适配。

### 1.2 核心设计理念

- **数据与图形分离**：业务数据存储在 DWG 命名对象字典（XRecord）中；图形图元仅通过 XData 携带唯一 ID 作为外键。卸载插件后图形仍可见，业务数据保留在图纸内。
- **非侵入式原生图元**：不使用自定义实体，全部采用 `REVCLOUD`、`MTEXT`、`TABLE` 等原生 CAD 图元。跨 CAD 兼容性以 v0.1.0 兼容性验证报告为准。
- **事件驱动一致性**：反应器监听图元增删改，采用“按 Database 隔离状态机 + 延迟队列”。回调内不开启事务，任务在主线程空闲或 `CommandEnded` 时批处理。
- **XRecord 为唯一业务真源，MTEXT 为投影**：任何编辑、同步、迁移必须遵循“先改 XRecord，再更新 MTEXT”的单向流。
- **取长补短**：保留原生图元兼容性，对标天正 T30、探索者易批注，将优秀交互与业务模型内化。
- **审计与可追溯**：所有批注变更写入链式哈希审计分片，支持 `NOTEAUDIT` 时间线追溯。
- **本地 AI 辅助**：基于 ONNX Runtime 的本地语义检索，不依赖网络。

### 1.3 竞品功能对标与设计借鉴

| 借鉴来源 | 功能设计 | CloudNote v0.1.0 落地方式 | 是否需网络 |
| --- | --- | --- | --- |
| 天正 T30 | 比例自适应弧长/字高 | 读取当前图形比例，自动计算默认值；天正比例深度适配 | 否 |
| 天正 T30 | 图层强制非打印 | `CAD_NOTE_TEXT` / `CAD_NOTE_CLOUD` 强制非打印 | 否 |
| 天正 T30 | 普通/手绘云线 | 新建批注提供两种云线样式选择 | 否 |
| 天正 T30 | 日期+文字注释 | MTEXT 自动包含序号、日期、摘要 | 否 |
| 探索者 | 批注回复机制 | XRecord 增加 `Replies` 分片，支持多轮本地对话 | 否 |
| 探索者 | 知识库/词库 | 本地 JSON 词库 + 快速检索匹配 + AI 语义检索 | 否 |
| 探索者 | 图纸内汇总表 | 图纸指定位置生成汇总表，平台兼容降级 | 否 |
| 探索者 | 框选过滤导出 | 选择集包围盒 + 条件过滤双维度导出 | 否 |
| 探索者 | 自动定位加载 | ZOOM Object + 临时选择集高亮 | 否 |
| AutoCAD COMPARE（2019+） | 版本差异联动 | 捕获 COMPARE 修订云线，按包围盒关联未解决批注；AutoCAD 2019-2026 启用，2014-2018 优雅降级提示 | 否 |

> **关键决策**：探索者网络协同（飞传、云端同步）为 v0.1.0 非目标，不纳入任何版本。批注回复、知识库、AI 语义检索、审计、审查包、批量合并均做纯本地实现。

---

## 2. 技术架构

### 2.1 技术选型

采用 **C# + AutoCAD .NET API**。托管方案满足批注业务性能，并降低开发、调试、多版本维护成本。

| 维度 | ObjectARX (C++) | .NET API (C#) | 结论 |
| --- | --- | --- | --- |
| 性能 | 极快 | 很快 | 批注场景均满足 |
| 开发效率 | 低 | 高 | C# 胜出 |
| 维护成本 | 高 | 低 | C# 胜出 |
| 人才储备 | 稀缺 | 充足 | C# 胜出 |

### 2.2 三层架构

```text
UI 层：WPF/WinForms + PaletteSet，不直接依赖 CAD API
核心业务层：纯 C#，批注增删改查、校验、导出、序号管理、审计、AI 语义、批量合并
平台适配层：唯一直接与 AutoCAD / 中望 / 浩辰 .NET API 交互的薄层
```

### 2.3 项目目录结构

项目采用 **“共享工程核心 (Core.shproj) + 多版本宿主外壳 (src/Rxx) + 集中依赖库 (Libs) + 统一构建输出 (build) + Inno Setup 一键安装器 (installer)”** 规范架构体系：

```text
CAD Auto CloudNote_(CAD 云线批注)/
├── CadAutoCloudNote.slnx                     // 现代 Visual Studio 统一解决方案文件
├── README.md                                 // 项目整体说明与使用指南
├── LICENSE.txt                               // 开源/商业授权协议
├── 云线批注.md                               // 软件开发白皮书与技术规范
│
├── CadAutoCloudNote.Core/                    // 核心业务共享项目 (.shproj + .projitems，不输出独立 DLL)
│   ├── AppConstants.cs                       // 全局常量（版本号、插件名、命令名、注册表路径）
│   ├── CadAutoCloudNote.Core.md              // Core 核心模块完整技术文档说明
│   ├── CadAutoCloudNote.Core.projitems       // 共享代码编译项配置
│   ├── CadAutoCloudNote.Core.shproj          // C# 共享项目工程文件
│   ├── Commands.cs                           // 极薄命令注册与分发层（仅声明 [CommandMethod]）
│   ├── PluginMain.cs                         // 插件生命周期入口、AssemblyResolve、SetDllDirectory
│   ├── Config/                               // 配置持久化管理
│   │   ├── ConfigManager.cs                  // 配置序列化与 AppData/图纸 XRecord 存取
│   │   └── NoteSettings.cs                   // 批注全局参数模型（含严重等级颜色映射、审查词典、健康体检）
│   ├── Helpers/                              // 底层交互辅助工具
│   │   ├── ScaleHelper.cs                    // 图纸比例与模型空间换算辅助类
│   │   ├── XDataHelper.cs                    // 图元 XData 读写唯一网关（含 EnsureRegApp）
│   │   └── XRecordHelper.cs                  // XRecord 4+2 分片存储与 50 条自动分卷算法
│   ├── Interfaces/                           // 跨版本解耦接口层
│   │   ├── ICadPlatformAdapter.cs            // 国产 CAD / AutoCAD 平台适配器抽象
│   │   ├── ICloudDrawer.cs                   // 云线绘制接口
│   │   ├── INoteRepository.cs                // XRecord 数据仓储抽象
│   │   ├── INoteUIProvider.cs                // UI 表现隔离接口（WPF / WinForms 隔离）
│   │   ├── IUpdateChecker.cs                 // 版本检查接口
│   │   └── IXDataAccessor.cs                 // XData 访问抽象
│   ├── Jigs/                                 // 交互式动态云线 Jig 引擎
│   │   ├── FreeCloudJig.cs                   // 徒手轨迹距离阈值采样云线绘制器
│   │   ├── PolygonCloudJig.cs                // 多边形凹凸闭合云线绘制器（Shoelace 顺逆时针判定与法向凸度自适应）
│   │   └── RectCloudJig.cs                   // 矩形两点动态云线绘制器
│   ├── Models/                               // 领域数据模型
│   │   ├── AuditEntry.cs                     // 链式哈希审计条目模型
│   │   ├── BatchReviewSummary.cs             // 批量多图合并分析模型
│   │   ├── CloudEntity.cs                    // 云线关联图元 Handle 模型
│   │   ├── DrawingInstance.cs                // 图纸指纹
│   │   ├── ModificationOrigin.cs             // 修改来源枚举
│   │   ├── NoteRecord.cs                     // 批注主体元数据模型
│   │   ├── NoteReply.cs                      // 批注回复模型
│   │   ├── ReviewPackageManifest.cs          // 离线审查包清单模型
│   │   ├── SemanticSearchConfig.cs           // AI 向量检索参数配置
│   │   └── TextEntity.cs                     // 批注文字 Handle 模型
│   ├── Protocols/                            // 协议与策略
│   │   ├── CapacityPolicy.cs                 // XRecord 容量与分卷策略
│   │   └── NoteLifecycleState.cs             // 批注生命周期状态
│   ├── Reactors/                             // 反应器与事务隔离引擎
│   │   ├── CloneDetection.cs                 // COPY/EXPLODE 候选队列与 CommandEnded 批处理
│   │   ├── DocumentState.cs                  // 单文档上下文与内部操作深度计数器
│   │   ├── PendingTask.cs                    // 延迟队列任务模型
│   │   ├── PendingTaskConflictResolver.cs    // Erase/Modify/Reply 任务冲突消除矩阵
│   │   └── ReactorManager.cs                 // 按 Database 隔离的反应器总控（复合图元级联删除与 Idle 消除残影）
│   ├── Resources/                            // 图标与菜单资源
│   │   ├── CadAutoCloudNote.cui              // 低版本菜单定义
│   │   ├── CadAutoCloudNote.cuix             // 高版本菜单/功能区定义
│   │   ├── icon.ico                          // 插件标准图标
│   │   └── icon.png
│   ├── Services/                             // 核心领域服务
│   │   ├── AuditService.cs                   // SHA256 链式防篡改哈希审计服务
│   │   ├── BatchReviewService.cs             // 批量 DWG 只读副本检索合并服务
│   │   ├── CompareLinkService.cs             // COMPARE 版本差异云线关联分析服务
│   │   ├── ExportService.cs                  // NPOI Excel / CSV 数据导出服务
│   │   ├── KnowledgeBaseService.cs           // 本地知识库 JSON 检索服务
│   │   ├── Logger.cs                         // 本地轻量化滚动异常日志服务
│   │   ├── MigrationService.cs               // 策略化数据迁移服务
│   │   ├── NoteService.cs                    // 批注生命周期与草稿短事务服务（含健康体检孤儿清理）
│   │   ├── ReplyService.cs                   // 回复追加与状态机流转服务
│   │   ├── ReviewPackageService.cs           // .cloudnotepkg 打包、解包与坐标校准服务
│   │   ├── SemanticSearchService.cs          // ONNX Runtime 语义嵌入与纯 C# 降级检索服务
│   │   ├── SummaryTableService.cs            // 图纸内 TABLE / 降级汇总表生成服务
│   │   ├── TianzhengScaleHelper.cs           // 天正与 AutoCAD 比例变量自适应服务
│   │   ├── UpdateChecker.cs                  // 版本检查服务
│   │   └── ValidationService.cs              // 数据有效性校验服务
│   ├── UI/                                   // 跨技术栈 UI 门面抽象
│   │   └── NoteUIProvider.cs
│   ├── UI.WinForm/                           // WinForms 界面（低版本 R17~R18 降级与兜底）
│   │   ├── ConfigForm.cs                     // WinForms 紧凑配置对话框
│   │   ├── NoteEditForm.cs                   // 批注新建与编辑表单
│   │   ├── NotePaletteControl.cs             // WinForms 版批注面板控件
│   │   └── PaletteManager.cs
│   └── UI.Wpf/                               // 现代化 WPF 界面（高版本 R19~R27）
│       ├── ConfigWindow.xaml                 // 五大板块紧凑设置管理窗口
│       ├── ConfigWindow.xaml.cs
│       ├── NoteEditDialog.xaml               // 批注新建与编辑对话框
│       ├── NoteEditDialog.xaml.cs
│       ├── NotePaletteControl.xaml           // PaletteSet 内嵌非模态管理面板
│       └── NotePaletteControl.xaml.cs
│
├── src/                                      // 多版本宿主外壳工程（纯宿主空壳，源码级吸入 Core）
│   ├── CadAutoCloudNote.R17/                 // 适配 AutoCAD 2007-2009 (net35, x86/AnyCPU, WinForms)
│   ├── CadAutoCloudNote.R18/                 // 适配 AutoCAD 2010-2012 (net35, x86/AnyCPU, WinForms)
│   ├── CadAutoCloudNote.R19/                 // 适配 AutoCAD 2013-2014 (net45 / net40)
│   ├── CadAutoCloudNote.R20/                 // 适配 AutoCAD 2015-2016 (net45)
│   ├── CadAutoCloudNote.R21/                 // 适配 AutoCAD 2017 (net46)
│   ├── CadAutoCloudNote.R22/                 // 适配 AutoCAD 2018 (net46)
│   ├── CadAutoCloudNote.R23/                 // 适配 AutoCAD 2019-2020 (net47)
│   ├── CadAutoCloudNote.R24/                 // 适配 AutoCAD 2021-2024 (net48)
│   ├── CadAutoCloudNote.R25/                 // 适配 AutoCAD 2025 (net8.0-windows)
│   ├── CadAutoCloudNote.R26/                 // 适配 AutoCAD 2026 (net8.0-windows)
│   └── CadAutoCloudNote.R27/                 // 适配 AutoCAD 2027 (net10.0-windows)
│
├── Libs/                                     // 集中管理的原生 SDK 与第三方库
│   ├── AutoCAD/                              // 原生 SDK 集合
│   │   ├── Net2.0_2007-2009/                 // R17 依赖库
│   │   ├── Net3.5_2010-2012/                 // R18 依赖库
│   │   ├── Net4.0_2013-2014/                 // R19 依赖库
│   │   ├── Net4.5_2015-2016/                 // R20 依赖库
│   │   ├── Net4.6_2017/                      // R21 依赖库
│   │   ├── Net4.6_2018/                      // R22 依赖库
│   │   ├── Net4.7_2019-2020/                 // R23 依赖库
│   │   ├── Net4.8_2021-2024/                 // R24 依赖库
│   │   ├── Net8.0_2025-2026/                 // R25-R26 依赖库
│   │   └── Net10.0_2027/                     // R27 依赖库
│   └── Dependencies/                         // 第三方依赖程序集
│       ├── NPOI/
│       └── OnnxRuntime/
│
├── build/                                    // 统一编译产物目录
│   ├── R17/ ~ R27/                           // 全 11 个版本编译输出 DLL 及依赖
│   ├── Release/                              // 稳定版发布包
│   └── Installer/                            // 安装程序输出目录
│
├── installer/                                // Inno Setup 一键智能安装分发包
│   ├── CAD Auto CloudNote-APP生成 .iss       // 快速生成脚本
│   ├── CadAutoCloudNote_Installer.iss        // 主打包脚本（含全系列注册表智能嗅探与静默注册）
│   ├── icon.ico                              // 安装包图标
│   ├── icon.png
│   ├── Readme.html                           // 图文使用帮助手册
│   └── 文件清单.txt                          // 物理文件清单规范
│
└── CADAutoCloudNote.bundle/                  // Autodesk Autoloader 即插即用部署包
    ├── PackageContents.xml
    └── Contents/
```

---

## 3. 数据模型

### 3.1 核心模型

一条批注的业务语义为“**N 朵云线 + 1 个批注文字 + 元数据 + 回复列表 + 审计链**”。

```csharp
public class NoteRecord
{
    public int SchemaVersion { get; set; } = 1;
    public string NoteId { get; set; }
    public int SerialNumber { get; set; }
    public string Content { get; set; }
    public string Author { get; set; }
    public string Discipline { get; set; }
    public DateTime CreatedTime { get; set; }
    public bool IsPrintable { get; set; }
    public string CloudStyle { get; set; }      // Normal / HandDrawn
    public string Status { get; set; }          // Open/Replied/Resolved/Closed
    public string SourceDrawingId { get; set; }
    public string PluginVersion { get; set; }
    public string StatusColor { get; set; }
}

public class CloudEntity
{
    public string Handle { get; set; }
    public int CloudIndex { get; set; }
    public string RegionDesc { get; set; }
    public DateTime CreatedTime { get; set; }
    public string StatusColor { get; set; }
}

public class TextEntity
{
    public string Handle { get; set; }
    public DateTime CreatedTime { get; set; }
}

public class NoteReply
{
    public string ReplyId { get; set; }
    public string Content { get; set; }
    public string Author { get; set; }
    public DateTime ReplyTime { get; set; }
}

public class DrawingInstance
{
    public string DrawingInstanceId { get; set; }
    public DateTime FirstOpenedTime { get; set; }
    public string FirstOpenedBy { get; set; }
    public string LastPluginVersion { get; set; }
}

public class AuditEntry
{
    public string AuditId { get; set; }
    public string NoteId { get; set; }
    public DateTime Timestamp { get; set; }
    public string Operator { get; set; }
    public string Action { get; set; }
    public string OldValue { get; set; }
    public string NewValue { get; set; }
    public string PreviousHash { get; set; }
    public string CurrentHash { get; set; }
}

public class ReviewPackageManifest
{
    public string PackageId { get; set; }
    public int PackageVersion { get; set; } = 1;
    public DateTime CreatedTime { get; set; }
    public string DrawingName { get; set; }
    public string DrawingId { get; set; }
    public double[] UcsOrigin { get; set; }
    public double[] UcsXAxis { get; set; }
    public double[] UcsYAxis { get; set; }
    public int NotesCount { get; set; }
    public string PackageHash { get; set; }
}

public class BatchReviewSummary
{
    public string DrawingPath { get; set; }
    public int NoteCount { get; set; }
    public int OpenCount { get; set; }
    public int ResolvedCount { get; set; }
    public string DuplicateGroupId { get; set; }
}

public class SemanticSearchConfig
{
    public string ModelPath { get; set; }
    public int EmbeddingDimension { get; set; } = 384;
    public double SimilarityThreshold { get; set; } = 0.65;
    public int TopK { get; set; } = 5;
}

public enum ModificationOrigin
{
    User,
    Plugin,
    ReactorRepair,
    UndoRedo,
    Clone,
    Migration,
    Audit,
    CompareLink,
    BatchMerge
}
```

### 3.2 XRecord 分片存储

| XRecord Key | 存储内容 | 说明 |
| --- | --- | --- |
| `NOTE::{GUID}` | `NoteRecord` JSON | 主体元数据 |
| `NOTE::{GUID}::CLOUDS` | `List<CloudEntity>` JSON | 云线关联列表 |
| `NOTE::{GUID}::TEXT` | `TextEntity` JSON | 单条批注文字 |
| `NOTE::{GUID}::REPLIES` | `List<NoteReply>` JSON | 回复列表，自动分卷 |
| `NOTE::{GUID}::REPLIES::PART{n}` | `List<NoteReply>` JSON | 回复分卷，每卷最多 50 条 |
| `NOTE::{GUID}::AUDIT` | `List<AuditEntry>` JSON | 审计链，自动分卷 |
| `NOTE::{GUID}::AUDIT::PART{n}` | `List<AuditEntry>` JSON | 审计分卷，每卷最多 50 条 |

**容量策略：**
- 硬阈值：单片 JSON 不超过 **32KB**（XRecord 字符串上限约 64KB，留 50% 安全余量）。
- 软阈值：单片 JSON 超过 **16KB** 时输出性能告警日志。
- 回复分卷：每卷最多 **50 条** `NoteReply`，超出自动创建 `REPLIES::PART{n}`。
- 审计分卷：每卷最多 **50 条** `AuditEntry`，超出自动创建 `AUDIT::PART{n}`。
- 读取策略：`NOTEQUERY` 只读主体，懒加载分片；`NOTEOUT`、`NOTESUMMARY`、`NOTEAUDIT`、`NOTEPKG` 批量读取全部分片。

### 3.3 全局字典结构

NOD 子字典 `CAD_CLOUD_NOTE` 下：

| Key | 类型 | 说明 |
| --- | --- | --- |
| `_SETTINGS` | XRecord | 全局配置 |
| `_COUNTER` | XRecord | 当前最大批注序号 |
| `_KB_POINTER` | XRecord | 本地词库 JSON 路径指针 |
| `_DRAWING_ID` | XRecord | `DrawingInstance` |
| `_SEMANTIC_CONFIG` | XRecord | `SemanticSearchConfig` |
| `NOTE::{GUID}` | XRecord | 批注主体 |
| `NOTE::{GUID}::CLOUDS` | XRecord | 云线分片 |
| `NOTE::{GUID}::TEXT` | XRecord | 文字分片 |
| `NOTE::{GUID}::REPLIES` | XRecord | 回复分片，可分卷 |
| `NOTE::{GUID}::REPLIES::PART{n}` | XRecord | 回复分卷 |
| `NOTE::{GUID}::AUDIT` | XRecord | 审计分片，可分卷 |
| `NOTE::{GUID}::AUDIT::PART{n}` | XRecord | 审计分卷 |

> **PURGE 保护**：始终保留 `_SETTINGS / _COUNTER / _KB_POINTER / _DRAWING_ID / _SEMANTIC_CONFIG`。

### 3.4 图元 XData

- AppName：`CAD_CLOUD_NOTE_LINK`
- 内容：单个 ASCII 字符串 = 批注 GUID
- 写入入口：仅限 `XDataHelper.SetApplicationXData`

### 3.5 全局配置 NoteSettings

```csharp
public class NoteSettings
{
    // 1. 图层与样式规范 (Layer & Style)
    public string CloudLayer { get; set; } = "CAD_NOTE_CLOUD";
    public string TextLayer { get; set; } = "CAD_NOTE_TEXT";
    public bool ForceNonPlotting { get; set; } = true;
    public int CloudColorIndex { get; set; } = 1;              // 云线默认颜色索引 (1=红色, 3=绿色, 256=ByLayer)
    public int TextColorIndex { get; set; } = 7;               // 文字默认颜色索引 (7=白/黑, 2=黄色)
    public string TextStyleName { get; set; } = "Standard";
    public double CloudWidth { get; set; } = 0.0;               // 多段线全局宽度 (0=细线, 0.3mm, 0.5mm, 0.8mm)
    public int CloudStyle { get; set; } = 1;                    // 0=普通等宽(Normal), 1=书法笔锋(Calligraphy)

    // 2. 云线几何与绘制 (Cloud Geometry)
    public double ArcLengthRatio { get; set; } = 6.0;           // 弧长相对图纸比例系数 (默认 6.0，契合原生自然云弧)
    public double BulgeCurvature { get; set; } = 0.520567;      // 圆弧凸度 (默认 0.520567 对应约 110 度圆弧)
    public int DefaultCloudType { get; set; } = 0;             // 默认绘制模式: 0=矩形, 1=多边形, 2=徒手
    public double MinArcLength { get; set; } = 3.0;             // 最小弧长保护下限 (防密集群弧卡死)
    public double MaxArcLengthRatio { get; set; } = 30.0;       // 最大弧长保护上限系数

    // 3. 文字与引线 (Text & Leader)
    public double TextHeightRatio { get; set; } = 3.5;         // 文字高度相对图纸比例系数 (默认 3.5)
    public double ArrowSizeRatio { get; set; } = 2.5;          // 引线箭头大小系数 (默认 2.5)
    public int LeaderType { get; set; } = 0;                   // 引线模式: 0=带箭头引线, 1=点引线, 2=无引线
    public string AutoNumberPrefix { get; set; } = "#";        // 批注编号前缀 ("#", "NOTE-")

    // 4. 业务与协同 (Discipline & Workflow)
    public string DefaultDiscipline { get; set; } = "建筑";     // 默认专业 (建筑、结构、给排水、暖通、电气、总图、通用)
    public string DefaultAssignee { get; set; } = Environment.UserName; // 默认负责人 / 审核人
    public string DefaultPriority { get; set; } = "一般";       // 优先级 (一般、重要、紧急)
    public bool AutoZoomOnSelect { get; set; } = true;          // 看板选中是否自动缩放定位
    public bool EnableSimpleNoteMode { get; set; } = true;      // 启用极简快速批注模式
    public string SimpleModeDefaultContent { get; set; } = "待修改"; // 极速模式默认意见
    public List<string> TeamMembers { get; set; }               // 团队成员名单字典

    // 5. 规范与 AI 智能 (Knowledge & AI)
    public bool EnableAiSemantic { get; set; } = false;         // 是否开启 AI 语义检索匹配
    public double AiSimilarityThreshold { get; set; } = 0.60;   // 语义匹配相似度阈值
    public bool AutoFillStandardCode { get; set; } = true;      // 自动填入匹配规范条文编号

    // 6. 存储与审计 (Storage & Audit)
    public bool EnableAuditTrail { get; set; } = true;          // 开启防篡改审计日志
    public int MaxNotesPerVolume { get; set; } = 50;            // 单卷最大容纳批注数 (分卷上限)
    public bool AutoBackupOnSave { get; set; } = true;          // 保存图纸时自动备份批注元数据

    // 7. 高级联动机制 (Advanced Linkage)
    public bool EnablePriorityColorLink { get; set; } = true;   // 开启严重等级与颜色智能联动
    public int PriorityColorNormal { get; set; } = 3;           // 一般优先级颜色 (3=绿色)
    public int PriorityColorImportant { get; set; } = 6;        // 重要优先级颜色 (6=品红)
    public int PriorityColorUrgent { get; set; } = 1;           // 紧急优先级颜色 (1=红色)
    public List<string> QuickReviewSnippets { get; set; }       // 常用审查意见快捷词典
}
```

---

## 4. 核心功能模块

### 4.1 新建批注（CLOUDNOTE / CNOTE / CN）

**事务边界**：长交互命令，采用 **PendingNote 草稿 + 短事务 + 最终确认**。

#### 4.1.1 命令生命周期

```text
CNOTE 开始
  ↓ 阶段 A：弹窗收集内容（普通模式）或读取默认意见（极速模式 CNS）
  ↓ 阶段 B：创建 PendingNote 草稿（仅内存，不占序号）
  ↓ 阶段 C：交互式云线绘制循环（矩形 / 多边形 / 徒手 / 对象转换）
  ↓ 阶段 D：放置批注引线与 MTEXT 文字，短事务写入
  ↓ 阶段 E：最终事务 commit 为正式 NoteRecord，自增序号，写入审计 Create
  ↓ 异常路径：ESC / 异常 / UNDO → 级联 Rollback 内存草稿与已绘制图元
```

#### 4.1.2 交互式动态云线绘制引擎（Jig 架构）

为摆脱原生 `REVCLOUD` 命令交互死板、无法动态预览、圆弧易反向的缺陷，系统内置了 3 大专用交互式 Jig 引擎：

1. **矩形动态云线（`RectCloudJig`，命令 `CNRECT`）**：
   - 交互模式：拾取对角两点，鼠标拖拽过程中实时重算包围矩形的四边云线圆弧；
   - 算法：沿矩形 4 条边界按当前比例自适应步长（`ArcLengthRatio * Scale`）均匀布设顶点，并赋予正向圆弧凸度（Bulge = 0.520567 约 110 度圆弧），外凸闭合。

2. **多边形凸凹云线（`PolygonCloudJig`，命令 `CNPOLY`）**：
   - 交互模式：连续拾取多边形拐点，支持随时回退与实时橡皮筋动态预览，回车或闭合拾取首点闭合；
   - **Shoelace 顺逆时针判定算法（鞋带定理/高斯面积公式）**：
     针对复杂凸/凹多边形，计算多边形有向面积：
     $$2 \cdot Area = \sum_{i=0}^{n-1} (x_i \cdot y_{i+1} - x_{i+1} \cdot y_i)$$
     若有向面积 > 0，则顶点序列为逆时针（CCW）；若 < 0，则为顺时针（CW）。
   - **法向外凸度自适应**：
     根据顶点绕向，自适应调整每段微圆弧多段线的 Bulge 符号：
     顺时针绕向时 Bulge 设为负值，逆时针绕向时 Bulge 设为正值。此机制彻底解决了传统算法在绘制复杂凹多边形或反向绘制多边形时，云线圆弧“朝内凹陷、翻转刺入多边形内部”的几何缺陷，确保所有圆弧始终平滑朝外凸起。

3. **徒手连续轨迹云线（`FreeCloudJig`，命令 `CNFREE`）**：
   - 交互模式：光标在图面上自由移动，系统根据移动距离阈值（`StepDistance = ArcLengthRatio * Scale * 0.8`）自动采样离散轨迹点；
   - 闭合判定：当光标移近起始点阈值范围内或单击右键时，自动首尾闭合生成圆弧多段线。

4. **已有对象转云线（`CNCV` / `CONVERTCLOUD`）**：
   - 支持用户直接拾取图纸中已有的闭合多段线（Polyline）、圆（Circle）、椭圆（Ellipse）或闭合样条曲线（Spline），按当前比例与弧长参数直接离散转化为标准云线，并立即附加批注元数据。

5. **追加云线（`CNAC` / `ADDCLOUD`）**：
   - 允许向已存在的批注追加一朵或多朵新云线，自动继承原批注 GUID、图层与严重等级颜色，形成复合多区域标注。

6. **极速批注模式（`CNS` / `FASTNOTE`）**：
   - 针对现场施工审图高频标记场景，跳过弹出输入对话框流程，直接以预设默认意见（如“待修改”）快速落图；后续用户可在非模态看板面板（`CNPALETTE`）中统一按需修改批注文字与属性。

#### 4.1.3 反应器屏蔽与状态机隔离

插件自身图元创建期间，`DocumentState.IsInternalOperation = true`。反应器回调第一行检查该状态，若为 true 则直接返回，不入队任何事件。该标志严格按 Document / Database 隔离，杜绝全局静态变量导致的并发污染。

### 4.2 增补 / 删减云线

**增补**：读取 GUID，交互绘制新 `REVCLOUD`，附加 XData，短事务读取 `CLOUDS` 分片 → 追加 → 写回 → 写入审计 `Modify`。  
**删减**：点选云线，同一短事务内先删除图元，再读取 `CLOUDS` 分片，移除 Handle，写回 → 写入审计 `Modify`。  
判断：云线清空但文字还在，保留主体；云线与文字全删，弹窗确认删除整条批注记录。

### 4.3 批注查询（NOTEQUERY）

1. 启动前置校验只读：遍历主体，读取分片，`db.TryGetObjectId` 校验句柄。失效句柄仅标记，不自动写回。  
2. 加载有效批注，展示 PaletteSet 非模态面板。  
3. 字段：序号、内容、批注人、专业、创建时间、状态、回复数、审计标识、差异关联。  
4. 筛选：关键词、批注人、专业、状态、AI 语义相似度。  
5. 交互：选中 → `ZOOM Object` + `SetImpliedSelection` 高亮；点击回复数懒加载。  
6. 双击编辑 → 走 `ModificationOrigin.Plugin` 写回 XRecord → 同步 MTEXT → 写入审计 `Modify`。

> **UI 宿主**：`PaletteSet` 本质为 WinForms 容器。优先使用 WinForms `UserControl`；若必须承载 WPF 控件，必须使用 `System.Windows.Forms.Integration.ElementHost`。不得使用 `WindowsFormsHost`，该类型用于“WPF 承载 WinForms”，方向相反。AutoCAD 2025+ 优先 WinForms。

### 4.4 批注回复（NOTEREPLY）

短事务读取 `REPLIES` 分片，追加 `NoteReply`，写回，更新主体 `Status`，写入审计 `Reply`。  
状态机：
- 初始 `Open`；
- 非原批注人回复 → `Replied`；
- 原批注人回复 → `Resolved`；
- 批注人手动关闭 → `Closed`；
- 已关闭可重新打开 → `Open`；
- 多轮回复按最后一次回复人身份更新。  

回复不写入 MTEXT；可追加“（已回复）”标记，必须走内部操作屏蔽反应器。

### 4.5 批注输出导出（NOTEOUT）

模式：全图 / 框选区域。  
框选：选择集 → 读 XData 收集 GUID → 云线包围盒相交纳入 → GUID 去重。  
导出字段：序号、内容、批注人、专业、创建时间、状态、关联云线数、WCS 包围盒、回复摘要、审计摘要。  
主路径 NPOI 输出 `.xlsx`；降级 CSV。前置二次过滤：状态、关键词、AI 语义相似度。

### 4.6 图纸内汇总表（NOTESUMMARY）

用户拾取插入点，读取有效批注按序号排序。优先 `TABLE`，不支持时降级 `LINE + MTEXT`。列：序号、内容、批注人、状态、回复数、审计标识。图层 `CAD_NOTE_SUMMARY`，默认非打印，可配置。重复执行时检测并更新已有汇总表，汇总表需写入专用 XData 标记。

### 4.7 知识库词库（NOTEKB）

唯一真源：`%AppData%\CADAutoCloudNote\knowledge_base.json`。  
JSON 结构：

```json
{
  "version": 1,
  "categories": [
    {
      "name": "结构专业",
      "items": [
        { "keyword": "配筋不足", "content": "该处配筋不满足计算要求，请复核。", "tags": ["结构", "配筋"], "embedding": [] }
      ]
    }
  ]
}
```

支持实时检索、另存词库、JSON 导入导出、AI 语义检索。并发保护：短读-改-写 + 文件锁。路径失效仅提示词库不可用，不影响批注读取编辑。

### 4.8 显隐控制（NOTESHOW）

| 图层名 | 用途 | 默认状态 |
| --- | --- | --- |
| `CAD_NOTE_TEXT` | 批注文字 MTEXT | 强制非打印 |
| `CAD_NOTE_CLOUD` | 云线 REVCLOUD | 强制非打印 |
| `CAD_NOTE_SUMMARY` | 汇总表 | 默认非打印，可配置 |
| `CAD_NOTE_COMPARE` | 版本对比差异云线 | 默认非打印，可配置 |

打印策略优先级：
1. `ForceNonPrintableLayer = true`（默认）：强制 `Plottable=false`，忽略 `DefaultPrintable`。
2. 用户关闭：由 `DefaultPrintable` 控制。
3. UI 明确提示优先级。

### 4.9 批注设置（CNS / CNOTESET）

为解决配置项过于分散、层级过深的问题，系统将全局与图纸设置统一重构整合为 **五大紧凑业务大板块**，并搭载 **三大高级联动机制**：

#### 4.9.1 五大紧凑大板块

1. **板块 1：对象与规则 (Target & Rule)**
   - 9 套批注使用对象与工程角色模板（设计自校/内部校对、专业负责人/专业审核、项目审图/专审、施工图会审、设计变更、监理通知、现场交底、竣工验收/竣工图、BIM 管综协同）；
   - 专属图层、编号前缀、专用颜色及打印控制（如内部校对强制不打印）；
   - 图纸健康体检与孤儿 XRecord 垃圾清理（一键修复未闭环引线与失效数据，保持图纸纯净）；
   - 导出当前角色模板专有差异报表。

2. **板块 2：绘图与样式规范 (Drawing & Style)**
   - 云线图层（`CAD_NOTE_CLOUD`）与文字图层（`CAD_NOTE_TEXT`）命名与配置；
   - 强制非打印图层（`ForceNonPlotting`）全局防误打硬开关；
   - 云线默认颜色索引（1=红色, 3=绿色, 256=ByLayer）与文字颜色索引（7=白色, 2=黄色）；
   - 文字样式（TextStyle）与全局多段线线宽（0=细线, 0.3mm, 0.5mm, 0.8mm 等）；
   - 书法笔锋样式（Calligraphy）与普通等宽样式（Normal）切换。

3. **板块 3：极速与短语 (Rapid & Phrase)**
   - 极速免弹窗批注开关（`EnableSimpleNoteMode`）；
   - 极速默认意见与 1~8 黄金短语盲打切换配置；
   - 60+ 条 7 大专业高频短语库集中维护与 CRUD。

4. **板块 4：规范知识库 (Knowledge & Code)**
   - 现行国家建筑与工程强制性条文规范库 CRUD 与快速检索；
   - 规范条文自动填充开关（`AutoFillStandardCode`）；
   - 本地知识库 JSON 路径配置与导入导出；
   - 基于词条相似度的模糊匹配引擎（Bigram Dice-Sørensen 算法）。

5. **板块 5：快捷与系统 (Shortcuts & System)**
   - 核心 24 项业务命令速查映射表与双列网格；
   - 严重等级与图元颜色联动映射表（一般-绿色/重要-品红/紧急-红色）；
   - 恢复系统默认配置按钮与快速排查入口；
   - 单卷分卷容量上限（默认 50 条）。

#### 4.9.2 三大高级联动机制 (Advanced Linkage Engine)

1. **联动 1：严重等级与图元颜色联动**  
   在批注设置中定义“一般=3(绿)、重要=6(品红)、紧急=1(红)”映射。当批注严重程度改变或新建批注时，系统根据 `GetPriorityColorIndex()` 自动计算并同步修改云线与引线实体的颜色，使图纸在模型空间中形成直观的轻重缓急视觉分层。

2. **联动 2：常用审查意见快捷词典流转联动**  
   词典在板块 3 集中管理后，与极速批注（CNQ）、标准新建批注对话框、非模态看板面板（CNP）双向打通。在图纸各处均可一键选用标准意见，杜绝录入错别字，提升审图统一规范性。

3. **联动 3：图纸健康体检与孤儿 XRecord 垃圾清理联动**  
   针对长期跨版本协同、图元意外删除导致的“数据残留”，设置界面内置“图纸健康体检”功能。执行 `NoteService.CompactDrawingDatabase()` 深度扫描 NOD 字典与图纸图元，自动识别并清理无关联图形实体的孤儿 XRecord，消除幽灵数据并压缩 DWG 体积。

### 4.10 复合图元一体化绑定与级联安全删除（CNDEL / NOTEDEL）

#### 4.10.1 复合图元关联关系

一条完整的工程批注在图面由 4 类图元组成有机整体：
- 区域边界：修订云线（Polyline，携带 XData GUID 外键）；
- 指示标识：引线（Leader，弱关联）；
- 端头定位：三角形指示标（Solid，弱关联）；
- 业务内容：批注文字（MText，携带 XData GUID 外键）。

#### 4.10.2 级联安全删除机制（Cascade Deletion）

传统插件常因用户误删单根引线或三角形端头，导致批注处于“残缺半死”状态。本系统在 `ReactorManager` 中建立级联安全监听：
1. 引线与三角形图元在生成时，其句柄在内存状态机建立弱引用反查字典；
2. 当设计人员使用 CAD 原生 `ERASE` 命令误删或主动删除引线、三角形或文字时，反应器即时识别所属批注 ID；
3. 系统将删除事件入队 `EraseTask`，在命令结束时触发级联联动：自动同步清理同一批注下的多余子构件，并从 NOD 字典安全删除对应的 XRecord 分片；
4. 若多朵云线中单朵被删，则仅删减分片 Handle，保留文字与批注主体；当全部构件均被移除时，彻底注销整条批注，保证图纸数据库无任何垃圾残留。

#### 4.10.3 夹点拖拽编辑防滞后与防残影机制（`Application.Idle`）

针对设计人员在移动批注文字或拖动夹点时容易误点击到引线端头、导致拖拽失败甚至图面残影的问题：
- 系统重构了夹点拖拽交互逻辑，将图元属性更新与引线重绘延迟绑定至 `Application.Idle` 空闲消息泵；
- 在用户拖拽移动期间不执行高开销的数据库重构，仅在光标释放并触发 Idle 时一次性提交事务，彻底消除重影与夹点粘滞，大幅提升操作顺畅度。

### 4.11 比例适配（NOTEADAPT）

| 策略 | 实现 | 适用场景 |
| --- | --- | --- |
| 修改 MTEXT 字高 | 修改 `MText.TextHeight` | 仅调整文字 |
| 删除重建云线 | 按原云线顶点重建 `REVCLOUD`，重算弧长 | 云线尺寸严重不匹配 |
| 仅改参数 | 修改云线弧长参数（若 API 支持） | 轻量调整 |

默认执行“修改 MTEXT 字高 + 删除重建云线”组合。天正比例深度适配见 4.19。

### 4.12 批注与 DWG 版本对比联动（NOTECOMPARE）

**目标**：利用 AutoCAD 2019+ 内置 `COMPARE` 命令，将差异区域自动关联到当前未解决批注。在本插件 v0.1.0 支持矩阵中，AutoCAD 2019-2026 原生启用该联动；AutoCAD 2014-2018 则优雅降级提示。

**流程**：
1. 用户执行 `NOTECOMPARE`，选择比较图形。
2. 插件调用 `COMPARE` 命令，并设置 `Settings` 生成修订云线。
3. 通过 `Editor.SelectLast()` 捕获比较产生的临时云线，读取其包围盒。
4. 读取所有 `Open` / `Replied` 状态批注的云线包围盒。
5. 按包围盒相交规则，建立“差异区域 → 批注”映射。
6. 在 PaletteSet 面板中新增“差异关联”列，显示关联的批注序号。
7. 支持“一键定位到差异区域”和“批量标记为待复核”。
8. 写入审计 `CompareLink`。

**技术要点**：
- `COMPARE` 命令自 AutoCAD 2019 引入；AutoCAD 2019-2026 均原生支持 `COMPARE` 并由插件启用版本差异联动。
- 对 AutoCAD 2014-2018（以及不支持原生 `COMPARE` 的国产 CAD 环境），`NOTECOMPARE` 必须捕获未知命令并做运行环境判断，给出“当前 CAD 版本（AutoCAD 2014-2018）不支持原生 COMPARE 命令（2019+ 支持），建议升级或使用审查包（.cloudnotepkg）/ 导出 Excel 对比”的优雅降级提示。P0 实测验证范围覆盖 AutoCAD 2014–2026。
- 比较云线图层为 `CAD_NOTE_COMPARE`，默认非打印。
- 包围盒相交使用 WCS 坐标，需处理 UCS 旋转。

### 4.13 PaletteSet 非模态批注管理面板（NOTEPANEL）

**目标**：将 NOTEQUERY 从模态窗口升级为 PaletteSet 非模态面板，支持边查询边绘图。

**面板组成**：
- 顶部：筛选栏（关键词、批注人、专业、状态、AI 语义搜索）。
- 中部：DataGrid 批注列表（序号、内容、批注人、专业、状态、回复数、审计标识、差异关联）。
- 右侧：回复编辑区、审计时间线入口、状态颜色图例。
- 底部：定位、编辑、删除、导出、审查包按钮。

**技术要点**：
- 使用 `PaletteSet` + `UserControl`，AutoCAD 2025+ 优先 WinForms。
- 若使用 WPF 控件，必须使用 `System.Windows.Forms.Integration.ElementHost` 承载，禁止使用 `WindowsFormsHost`。
- 处理 `DocumentActivated` 事件，文档切换时刷新列表。
- 选中批注时通过 `Editor.SetImpliedSelection()` 实时高亮。
- 所有写操作走 `DocumentLock` + 短事务。

### 4.14 批注审查状态可视化（NOTECOLOR）

**目标**：按状态着色文字与云线，提升视觉区分度。

**实现**：
- `NoteSettings.StatusColorMap` 定义状态颜色。
- 修改 `MText.Color` 和 `Polyline.Color`，同步 `NoteRecord.StatusColor` 与 `CloudEntity.StatusColor`。
- 颜色修改包裹在 `EnterInternalOperation()` 中，避免触发 `ObjectModified`。
- PaletteSet 中显示颜色图例。
- 支持批量“一键标记为已解决”并自动更新颜色。
- 写入审计 `Resolve`。

### 4.15 AI 辅助批注语义审查（NOTEAISEARCH）

**目标**：基于 ONNX Runtime 的本地语义检索，推荐知识库条目。

**实现**：
- 知识库 JSON 中 `items` 增加 `tags` 和 `embedding` 字段。
- 首次启用时，使用 ONNX 模型（`all-MiniLM-L6-v2` 量化版，约 20MB）为所有条目生成 embedding，缓存到 `%AppData%\CADAutoCloudNote\models\`。
- 用户输入批注内容时，实时计算 embedding，与知识库条目计算余弦相似度，返回 Top-5 推荐。
- `SemanticSearchConfig` 存储模型路径、维度、阈值、TopK。
- 完全本地运行，不依赖网络。

**技术要点**：
- `Microsoft.ML.OnnxRuntime` 托管程序集放入 `deps`，通过 `AssemblyResolve` 动态加载。
- 其原生 `onnxruntime.dll` 及依赖非托管 DLL 必须在 `IExtensionApplication.Initialize()` 最前置通过 Win32 API `SetDllDirectory(depsPath)` 注入系统 DLL 搜索路径。`AssemblyResolve` 仅对托管程序集生效，不会拦截 P/Invoke 非托管 DLL。
- 模型文件作为 bundle 资源，首次加载时解压。
- 推理耗时目标 ≤ 200ms。
- 若模型加载失败或原生库缺失，降级为关键词检索，不影响批注主流程。

### 4.16 批注变更日志与不可篡改审计（NOTEAUDIT）

**目标**：记录所有批注变更，支持审计追溯。

**实现**：
- 新增 XRecord 分片 `NOTE::{GUID}::AUDIT`，存储 `List<AuditEntry>`。
- 每次写操作（Create/Modify/Reply/Resolve/Delete）追加审计记录。
- `CurrentHash = SHA256(PreviousHash + JSON(AuditEntry without CurrentHash))`，形成链式哈希。
- 任何历史记录篡改会导致后续所有哈希失效。
- `NOTEAUDIT` 命令以时间线形式展示批注完整变更历史。
- 导出时可选包含审计日志。
- 审计分片每 50 条自动分卷。

**技术要点**：
- 审计写入必须与主体写操作在同一事务中完成。
- 链式哈希计算使用 `System.Security.Cryptography.SHA256`。
- 序列化 `AuditEntry` 时排除 `CurrentHash` 字段，避免自引用。
- 审计分片读取懒加载，时间线展示时批量读取。

### 4.17 批注审查包导出与导入（NOTEPKG / NOTEIMPORT）

**目标**：生成可离线审阅的 `.cloudnotepkg` 文件，接收方无需安装插件即可查看。

**包结构**：
```text
review.cloudnotepkg (ZIP)
├── manifest.json          // ReviewPackageManifest
├── notes.json             // 全部批注数据（元数据 + 包围盒坐标）
├── preview.png            // 当前图纸预览截图
└── viewer.html            // 独立 HTML 查看器
```

**导出流程**：
1. 执行 `NOTEPKG`，选择保存路径。
2. 收集所有有效批注数据。
3. 生成预览截图（`Editor.CapturePreview` 或屏幕截图）。
4. 生成 HTML 查看器（内嵌 JSON + fabric.js 绘制包围盒）。
5. 计算包哈希，写入 `manifest.json`。
6. 打包为 ZIP。

**导入流程**：
1. 执行 `NOTEIMPORT`，选择 `.cloudnotepkg`。
2. 读取 `manifest.json`，校验包哈希。
3. 按坐标映射将批注导入当前图纸（支持 UCS 偏移校准）。
4. 生成新 `NoteId` 和序号，写入 XRecord。
5. 写入审计 `Migration`。

**技术要点**：
- 坐标映射需处理 WCS/UCS 转换和图纸偏移。
- 导入时允许用户手动指定基准点进行对齐。
- HTML 查看器使用 `fabric.js` 或 `Leaflet`，无需网络。

### 4.18 批量多图纸批注合并与差异分析（NOTEBATCH）

**目标**：对多个 DWG 文件批量读取批注，合并汇总并识别差异。

**流程**：
1. 执行 `NOTEBATCH`，选择多个 DWG 文件。
2. 对每个文件使用 `Database.ReadDwgFile()` 打开只读副本。
3. 读取所有批注的 XRecord（不加载图形）。
4. 按 `SerialNumber` + `Content` 归一化相似度 ≥ 0.8（或编辑距离 ≤ 内容长度的 20%）识别跨图纸相同批注。
5. 输出“跨图纸批注汇总表”，标注每条批注在各图纸中的存在状态。
6. 支持“批量导出为单个 Excel，多 Sheet 按图纸分组”。
7. 审计记录写入当前活动文档的汇总日志或独立审计文件；只读副本不修改。

**技术要点**：
- 批量读取使用 `StartOpenCloseTransaction`，每批 ≤ 50 个文件。
- 模糊匹配可使用 `FuzzySharp` 或自实现 Levenshtein。
- 汇总表列：批注序号、内容、出现图纸数、状态分布、重复组 ID。

### 4.19 天正比例深度适配（NOTEADAPT 增强）

**目标**：准确读取天正比例变量，优化新建批注的弧长与字高。

**实现**：
- 封装 `TianzhengScaleHelper`，优先读取天正自定义系统变量 `TCH_SCALE`（兼容 `TPSCALE` 等版本差异，P0 实测确定）。
- 读取失败时回退到 `VIEWSIZE / 30` 的通用算法。
- 同时读取 AutoCAD 的 `CANNOSCALE` 和 `DIMSCALE` 作为交叉验证。
- `NoteSettings.ScaleSource` 支持 `Auto/Tianzheng/AutoCAD/Manual`。
- P0 必须实测天正 T30 建筑/结构/给排水子版本的变量名和取值格式。

### 4.20 完整业务命令速查表（23 项核心业务命令）

系统为全生命周期操作提供了完整的 CAD 控制台业务命令，支持长命令、前缀统一的标准命令以及超短别名，适配不同设计习惯：

| 模块分类 | 业务命令 | 短别名 / 兼容别名 | 说明与功能定位 |
| --- | --- | --- | --- |
| **批注核心** | `CNOTE` | `CN`, `CLOUDNOTE` | 标准交互式新建批注（支持选择绘制模式、录入专业与责任人） |
| | `CNS` | `CNOTESET`, `NOTESET` | 打开批注设置中心（五大紧凑业务板块与三大高级联动） |
| | `CNP` | `CNOTEPANEL`, `NOTEPANEL` | 展开或激活 PaletteSet 非模态批注管理看板（双击定位/状态过滤） |
| | `CNQ` | `NOTEQUICK`, `CNQUICK` | 极速快速批注（一键回车免弹窗以默认意见落图，支持 1~8 黄金短语） |
| | `CNH` | `CNHELP`, `NOTEHELP` | 打开离线帮助文档与图文手册（快捷键 F1） |
| **几何云线** | `CNRECT` | 无 | 两点交互式动态矩形云线 Jig 绘制 |
| | `CNPOLY` | 无 | 多点凸凹闭合多边形云线 Jig 绘制（内置 Shoelace 算法自适应外凸） |
| | `CNFREE` | 无 | 徒手连续光标轨迹采样云线 Jig 绘制 |
| | `CNCV` | `CONVERTCLOUD` | 拾取图纸闭合实体（多段线/圆/椭圆）直接转换为批注云线 |
| | `CNAC` | `ADDCLOUD` | 拾取已有批注并为其追加新云线 |
| **协同流转** | `CNREPLY` | `NOTEREPLY` | 为选定批注追加设计回复与流转意见 |
| | `CNEDIT` | 无 | 编辑选定批注的文字内容、责任专业与优先级 |
| | `CNDEL` | `NOTEDEL` | 级联安全删除批注（图元与 XRecord 数据彻底闭环清理） |
| | `CNSTAT` | `NOTECOLOR` | 批注审查状态/严重等级可视化着色与图层更新 |
| **数据与输出** | `CNOUT` | `NOTEOUT` | 批注清单数据导出（支持 NPOI 原生 Excel .xlsx 及 CSV 降级） |
| | `CNSUM` | `NOTESUMMARY` | 在图纸模型空间指定位置插入或刷新原生批注汇总表格 |
| | `CNPKG` | `NOTEPKG` | 离线审阅包打包导出（.cloudnotepkg 格式，含 HTML 查看器） |
| | `CNIMP` | `NOTEIMPORT` | 离线审阅包导入并校验坐标对齐映射 |
| | `CNBATCH` | `NOTEBATCH` | 批量多图纸只读副本合并检索与跨图差异分析 |
| **审计与维护** | `CNAUDIT` | `NOTEAUDIT` | 调阅 SHA256 链式防篡改防抵赖时间线审计日志 |
| | `CNCHK` | `NOTECHECK` | 图纸健康体检，一键清理无关联实体的孤儿 XRecord 垃圾 |
| | `CNCOMP` | `NOTECOMPARE` | 与 AutoCAD 原生 COMPARE 修订云线建立差异区域智能关联 |
| | `CNADAPT` | `NOTEADAPT` | 图纸出图比例自适应调整（自动重算云线弧长与 MText 字高） |
| | `CNKB` | `NOTEKB` | 本地审查知识库维护、条文检索与 AI 向量嵌入管理 |

---

## 5. 数据一致性保障

### 5.1 反应器架构：按 Database 隔离 + 延迟队列

核心原则：
1. 每个 Database 独立 `DocumentState`，禁止全局单例。
2. 反应器回调只入队，不执行。
3. 任务在 `CommandEnded` / `Application.Idle` 主线程空闲时批量处理。
4. 插件自身操作通过文档级标志位屏蔽。

**技术根因**：ObjectARX 的 reactor notification 机制要求回调内不得嵌套事务。通知发生时数据库处于“通知锁”状态，嵌套事务会破坏事务管理器重入计数，导致 AutoCAD 致命错误。`ObjectId.GetObject()` 同样会导致崩溃，`ObjectId.Open()` 虽不崩溃但会引发对象显示异常、事件处理链断裂。因此必须严格入队。

```csharp
public class DocumentState
{
    public Document Document { get; set; }
    public Database Database { get; set; }
    public int InternalOperationDepth { get; set; }
    public PendingNote Draft { get; set; }
    public Queue<PendingTask> PendingTasks { get; set; }
    public List<object> RegisteredReactors { get; set; }
    public string ActiveCommand { get; set; }

    public bool IsInternalOperation => InternalOperationDepth > 0;

    public bool IsCommandActive(string commandName)
    {
        return string.Equals(ActiveCommand, commandName, StringComparison.OrdinalIgnoreCase);
    }

    public IDisposable EnterInternalOperation()
    {
        InternalOperationDepth++;
        return new DisposableAction(() => InternalOperationDepth--);
    }
}

public class ReactorManager
{
    private readonly Dictionary<Database, DocumentState> _states = new();
    public DocumentState GetState(Database db) { ... }
    public void Register(Document doc) { ... }
    public void Unregister(Database db) { ... }
    public void UnregisterAll() { ... }
    public void Enqueue(Database db, PendingTask task) { ... }
}
```

`ActiveCommand` 由 `CommandWillStart` / `CommandEnded` 维护。

### 5.2 延迟队列冲突解决机制

```csharp
public class PendingTask
{
    public string TaskId { get; set; }
    public string NoteId { get; set; }
    public string TaskType { get; set; }
    public int Priority { get; set; }
    public long SequenceNumber { get; set; }
    public DateTime EnqueueTime { get; set; }
}
```

**冲突矩阵**：

| 先入队任务 | 后入队任务 | 解决策略 |
| --- | --- | --- |
| Erase | Modify | Erase 优先；Modify 丢弃 |
| Erase | Reply | Erase 优先；Reply 丢弃 |
| Erase(EXPLODE) | Modify / Reply | 不自动剥离；标记 `OrphanCloneCandidate`，交 `NOTECHECK` 或用户确认 |
| Modify | Modify | 合并为最后一次状态，保留最高 SequenceNumber |
| Append | Erase | 按 SequenceNumber 顺序执行；若 Erase 后 Append，标记孤立候选 |
| Reply | Resolve | 合并为 Resolve，保留回复内容 |

**去重/合并逻辑**：
- 同 `NoteId` 的多个 `Modify` 任务在 `CommandEnded` 批处理入口合并。
- 同 `NoteId` 的 `Erase` 任务优先级最高，执行后清除同 NoteId 其他任务。
- 冲突无法自动解决时，标记 `OrphanCloneCandidate`，输出警告日志，交由 `NOTECHECK` 处理。

### 5.3 ObjectErased 监听与复合图元级联响应

```csharp
private void OnObjectErased(object sender, ObjectErasedEventArgs e)
{
    var db = (Database)sender;
    var state = ReactorManager.GetState(db);
    if (state.IsInternalOperation) return;

    if (e.DBObject is not Entity entity) return;
    
    // 1. 获取主实体绑定的 NoteId
    var noteId = XDataHelper.GetNoteId(entity);
    
    // 2. 复合图元弱引用反查：若实体为引线 (Leader) 或端头三角形 (Solid)，通过句柄缓存反查所属 NoteId
    if (string.IsNullOrEmpty(noteId))
    {
        noteId = state.FindNoteIdBySubEntityHandle(entity.Handle.ToString());
    }
    if (string.IsNullOrEmpty(noteId)) return;

    // EXPLODE 期间不得直接按普通 Erase 处理，否则会误剥离 XRecord 分片
    if (state.IsCommandActive("EXPLODE"))
    {
        ReactorManager.Enqueue(db, new ExplodeEraseCandidate(
            noteId, entity.Handle.ToString()));
        return;
    }

    // 入队级联删除任务：在 CommandEnded 时协同清理同组引线/三角形/文字及 XRecord
    ReactorManager.Enqueue(db, new EraseTask(noteId, entity.Handle.ToString()));
}
```

### 5.4 ObjectModified 监听与夹点编辑防滞后机制

```csharp
private void OnObjectModified(object sender, ObjectEventArgs e)
{
    var db = (Database)sender;
    var state = ReactorManager.GetState(db);
    if (state.IsInternalOperation) return;

    // 1. 批注文字内容同步
    if (e.DBObject is MText mText)
    {
        var noteId = XDataHelper.GetNoteId(mText);
        if (!string.IsNullOrEmpty(noteId))
        {
            ReactorManager.Enqueue(db, new ModifyTask(
                noteId, mText.Handle.ToString(), mText.Contents, ModificationOrigin.User));
        }
    }

    // 2. 夹点拖拽编辑防滞后与防残影处理：
    // 当实体处于夹点变换中，注册 Application.Idle 单次钩子，等待拖拽完成后一次性刷新引线与图面重绘
    if (state.IsGripEditing)
    {
        state.RequestIdleRefresh();
    }
}
```

### 5.5 克隆检测：候选队列 + CommandEnded 批处理

```csharp
private void OnObjectAppended(object sender, ObjectEventArgs e)
{
    var db = (Database)sender;
    var state = ReactorManager.GetState(db);
    if (state.IsInternalOperation) return;

    if (e.DBObject is not Entity entity) return;
    var noteId = XDataHelper.GetNoteId(entity);
    if (string.IsNullOrEmpty(noteId)) return;

    ReactorManager.Enqueue(db, new AppendCandidate(
        noteId, entity.Handle.ToString(), entity.GetType().Name));
}
```

`CommandEnded` 批处理：
1. 按 `NoteId` 分组，收集候选图元 Handle 与类型。
2. 读取原批注 `CLOUDS` 与 `TEXT` 分片。
3. 整条复制判定：分组包含原批注全部 `CLOUDS` Handle 和 `TEXT` Handle，且 `NoteId` 已存在 → 生成新 `NoteId`、新序号，复制主体、CLOUDS、TEXT，清空 REPLIES，复制 AUDIT 并追加 `Clone` 记录，更新候选图元 XData 与 MTEXT 序号。
4. 部分复制判定：仅包含部分云线或仅文字 → 不静默清除 XData，标记为 `OrphanCloneCandidate`，输出警告日志，交由 `NOTECHECK` 或用户确认处理。
5. `NoteId` 不存在 → 交由 `NOTECHECK` 处理。

### 5.6 事务管理规范

1. 所有写 DWG 操作必须 `StartTransaction()` + `using` + `tr.Commit()`。
2. 只读批量查询可用 `StartOpenCloseTransaction`；写操作禁止使用。
3. 反应器回调禁止开启事务，只入队。
4. CLOUDNOTE 采用 PendingNote + 短事务 + 最终确认。
5. 所有写操作必须加 `DocumentLock`，运行在 CAD 主线程。
6. 审计写入必须与主体写操作同一事务。

### 5.7 边界场景处理矩阵

| 场景 | 处理方案 |
| --- | --- |
| 用户 ERASE 云线 | ObjectErased 入队，延迟队列清理分片，写入审计 |
| 用户双击修改 MTEXT | ObjectModified 入队，延迟队列同步主体 Content，写入审计 |
| COPY / COPYCLIP 复制批注 | ObjectAppended 候选队列 + CommandEnded 批处理重建，写入审计 |
| 仅复制云线或仅复制文字 | 标记孤立候选，警告日志，NOTECHECK 处理 |
| INSERT 图块含批注 | v0.1.0 策略化迁移 |
| WBLOCK | v0.1.0 用户显式选择是否带批注 |
| XREF | 只读，禁止在 Xref 编辑空间创建批注 |
| BIND | v0.1.0 迁移为本地批注 |
| EXPLODE | 用户炸开带批注图元：ObjectErased 检测到 `EXPLODE` 活动时转 `ExplodeEraseCandidate`；CommandEnded 不自动从 `CLOUDS` / `TEXT` 分片剥离 Handle，标记 `OrphanCloneCandidate`，输出警告，交 `NOTECHECK` 或用户确认 |
| PURGE | 保留 5 条哨兵 XRecord |
| UNDO / REDO | 基础同步 + 精细校验 + NOTECHECK 兜底 |
| 多文档切换 | 按 Database 隔离 DocumentState |
| 图纸比例变更 | 已存在批注不自动适配，用户调用 NOTEADAPT |
| Schema 版本高于插件 | NOTEQUERY 只读展示，提示升级，不修改原始数据 |
| 版本对比 | NOTECOMPARE 关联差异区域，不自动修改批注状态 |
| 审查包导入 | 坐标映射 + UCS 校准，生成新 NoteId |
| 批量多图合并 | 只读副本，不修改原图 |

### 5.8 Undo / Redo 处理

- 监听 `ModifyUndone` / `Reappended` / `Unappended` 与命令生命周期。
- `CommandEnded` 后执行轻量校验。
- 复杂跨命令 Undo 链由 `NOTECHECK` 最终修复。
- 完成精细同步增强。

### 5.9 多文档生命周期

```csharp
Application.DocumentManager.DocumentActivated += OnDocumentActivated;

private void OnDocumentActivated(object sender, DocumentCollectionEventArgs e)
{
    ReactorManager.Register(e.Document);
}

Application.DocumentManager.DocumentToBeDestroyed += OnDocumentClosing;

private void OnDocumentClosing(object sender, DocumentCollectionEventArgs e)
{
    ReactorManager.Unregister(e.Document.Database);
}
```

### 5.10 脏数据主动校验修复（NOTECHECK 四象限）

| XRecord | XData | 状态 | 处理 |
| --- | --- | --- | --- |
| 有 | 有 | 正常 | 无需处理 |
| 有 | 无 | XRecord 悬空 | 从分片中剔除该 Handle |
| 无 | 有 | 孤立批注图元 | 清除图元 XData，或标记待认领 |
| 无 | 无 | 普通图元 | 忽略 |

“XData 有”特指 `CAD_CLOUD_NOTE_LINK`，其他插件 XData 不动。  
`NOTECHECK`：遍历图元收集链接，遍历 XRecord 读取分片，按四象限修复，输出日志到 `%AppData%\CADAutoCloudNote\logs\`。

---

## 6. 技术要点与最佳实践

### 6.1 项目配置与 Shared Project 编译架构

项目全面对齐 `CAD Auto SNAP` 的 **Shared Project（共享项目）** 架构模式，解决单一 DLL 无法跨越 CLR 2.0/4.0/CoreCLR 版本的断代难题：

#### 6.1.1 核心共享工程 (CadAutoCloudNote.Core.shproj)
- 采用 C# Shared Project 模式，**不单独编译生成 DLL**；
- 承载全套业务模型、反应器、服务与 UI 交互逻辑；
- 被各个宿主外壳工程在编译期通过 `<Import Project="..\..\CadAutoCloudNote.Core\CadAutoCloudNote.Core.projitems" Label="Shared" />` 直接源码级“吸入”；
- 规避了任何运行时 Assembly BindingRedirect 失败与 MissingMethodException。

#### 6.1.2 多版本宿主外壳配置 (src/CadAutoCloudNote.Rxx.csproj)
宿主工程仅负责声明平台目标、定义预处理宏与引用对应版本 SDK：

| 宿主项目 | 目标 CAD 版本 | TargetFramework | 预处理宏 (DefineConstants) | SDK 引用路径 |
| --- | --- | --- | --- | --- |
| `CadAutoCloudNote.R17` | AutoCAD 2007-2009 | `net35` | `CAD_R17;USE_CONFIGFORM` | `Libs/AutoCAD/Net2.0_2007-2009/` |
| `CadAutoCloudNote.R18` | AutoCAD 2010-2012 | `net35` | `CAD_R18;USE_CONFIGFORM` | `Libs/AutoCAD/Net3.5_2010-2012/` |
| `CadAutoCloudNote.R19` | AutoCAD 2013-2014 | `net45` | `CAD_R19;USE_WPF` | `Libs/AutoCAD/Net4.0_2013-2014/` |
| `CadAutoCloudNote.R20` | AutoCAD 2015-2016 | `net45` | `CAD_R20;USE_WPF` | `Libs/AutoCAD/Net4.5_2015-2016/` |
| `CadAutoCloudNote.R21` | AutoCAD 2017 | `net46` | `CAD_R21;USE_WPF` | `Libs/AutoCAD/Net4.6_2017/` |
| `CadAutoCloudNote.R22` | AutoCAD 2018 | `net46` | `CAD_R22;USE_WPF` | `Libs/AutoCAD/Net4.6_2018/` |
| `CadAutoCloudNote.R23` | AutoCAD 2019-2020 | `net47` | `CAD_R23;USE_WPF` | `Libs/AutoCAD/Net4.7_2019-2020/` |
| `CadAutoCloudNote.R24` | AutoCAD 2021-2024 | `net48` | `CAD_R24;USE_WPF` | `Libs/AutoCAD/Net4.8_2021-2024/` |
| `CadAutoCloudNote.R25` | AutoCAD 2025 | `net8.0-windows` | `CAD_R25;USE_WPF` | `Libs/AutoCAD/Net8.0_2025-2026/` |
| `CadAutoCloudNote.R26` | AutoCAD 2026 | `net8.0-windows` | `CAD_R26;USE_WPF` | `Libs/AutoCAD/Net8.0_2025-2026/` |

#### 6.1.3 构建与依赖规范
- **SDK 引用声明**：所有 `AcCoreMgd.dll`、`AcDbMgd.dll`、`AcMgd.dll` 引用必须显式设置 `<Private>False</Private>`。
- **输出统一归流**：每个宿主工程的 `<OutputPath>` 统一配置为 `..\..\build\Rxx\`，并设置 `<AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>`。
- **现代 C# 语法 Polyfill**：Core 层采用 C# 10+ 语法（如 `record`, `init`, 模式匹配），在编译 `net45`~`net48` 时通过 PolySharp 或轻量 Polyfill 提供缺失的类型定义。
- **非托管依赖搜索路径注入**：针对 ONNX Runtime 等原生 C++ 动态库，在 `PluginMain.Initialize()` 中第一时间调用 Win32 `SetDllDirectory(depsPath)` 注入进程搜索路径。
- **写 DWG 操作加锁**：所有写操作必须加 `DocumentLock`，并运行在 CAD 主线程。

### 6.2 XDataHelper（唯一入口）

```csharp
public static class XDataHelper
{
    public const string AppName = "CAD_CLOUD_NOTE_LINK";

    private static void EnsureRegApp(Database db, Transaction tr) { ... }

    public static string GetNoteId(Entity entity) { ... }

    public static void SetNoteId(Entity entity, string noteId, Transaction tr)
    {
        EnsureRegApp(entity.Database, tr);
        SetApplicationXData(entity, AppName, new ResultBuffer(
            new TypedValue((int)DxfCode.ExtendedDataRegAppName, AppName),
            new TypedValue((int)DxfCode.ExtendedDataAsciiString, noteId)), tr);
    }

    public static void SetNoteId(Entity entity, string noteId)
    {
        using var tr = entity.Database.TransactionManager.StartTransaction();
        SetNoteId(entity, noteId, tr);
        tr.Commit();
    }

    public static void RemoveNoteId(Entity entity) { ... }
    public static ResultBuffer GetApplicationXData(Entity entity, string appName) { ... }
    public static void SetApplicationXData(Entity entity, string appName, ResultBuffer values, Transaction tr) { ... }
    public static void RemoveApplicationXData(Entity entity, string appName) { ... }
}
```

规范：全项目搜索 `entity.XData =`，只允许出现在 `XDataHelper` 内部。

### 6.3 XRecord 读写

写入时使用 `WriteXRecord(tr, dict, key, json)`。主体、云线、文字、回复、审计分片分别写入。回复与审计分片超限自动分卷。读取时按需懒加载。

### 6.4 比例自适应

区分 Annotation Scale、Viewport Custom Scale、Plot Scale、天正比例变量。统一换算函数 `ScaleHelper.PaperToModel`、`GetDefaultArcLength`。新建批注才执行自适应；已存在批注不自动跟随；用户调用 `NOTEADAPT` 批量重算。天正比例变量名（`TCH_SCALE` / `TPSCALE` 等）在 P0 内实测关闭。

### 6.5 性能优化

1. `SelectionSet` 用 `ToArray()` 一次性获取。
2. 大批量只读用 `StartOpenCloseTransaction`。
3. 导出先内存组装，再一次性写入。
4. 反应器延迟队列批量提交。
5. `NOTEQUERY` 懒加载分片。
6. AI 语义检索使用缓存 embedding。
7. 批量多图每批 ≤ 50 个文件。

### 6.6 日志体系

路径：`%AppData%\CADAutoCloudNote\logs\`。记录反应器事件、延迟队列、数据修复、Schema 迁移、导出异常、审计、AI 检索、批量合并、堆栈。按日期滚动，保留 30 天。`NOTELOG` 一键打开日志目录。使用 Serilog。

### 6.7 性能基准

| 场景 | 目标 |
| --- | --- |
| 单条批注创建（云线 + MTEXT + XRecord + 审计） | ≤ 2 秒 |
| NOTEQUERY 加载 500 条批注 | ≤ 1.5 秒 |
| 反应器延迟队列单次 CommandEnded 批处理 | ≤ 200ms |
| NOTECHECK 全图校验 1000 条批注 | ≤ 5 秒 |
| AI 语义检索单次查询 | ≤ 200ms |
| 审查包导出 500 条批注 | ≤ 3 秒 |
| 批量多图读取 50 个文件 | ≤ 10 秒 |

### 6.8 多包独立加载与系列隔离预案

**已知问题**：在 Autoloader 架构下，若 `SeriesMin` 与 `SeriesMax` 边界重叠或未严格限定，AutoCAD（特别是过渡版本如 AutoCAD 2025）可能同时扫描并加载 .NET Framework 与 .NET 8 两个 DLL，导致命令重复注册或状态冲突。此外，AutoCAD 2014-2020 亦必须防止误加载更高版本框架（如 net48）引发类型加载异常。

**预案**：
1. **Series 无缝无重叠划分**：
   - Net45 包（AutoCAD 2014-2020）：`SeriesMin="R19.1"` / `SeriesMax="R23.1"`
   - Net48 包（AutoCAD 2021-2024）：`SeriesMin="R24.0"` / `SeriesMax="R24.3"`
   - Net8 包（AutoCAD 2025-2026）：`SeriesMin="R25.0"` / `SeriesMax="R25.1"`
   三包在系列号维度严格无重叠拼接，由 AutoCAD Autoloader 引擎在应用包发现期实现第一道原生隔离。
2. 在 `PackageContents.xml` 中显式设置 `LoadOnAutoCADStartup="False"`，且必须声明完整 `<Commands GroupName="CADNOTE">` 节点；否则 AutoCAD 既不会随启动加载，也不会按命令触发按需加载。若目标 SDK 对属性位置敏感，按实测移至 `ComponentEntry`。
3. 在 `IExtensionApplication.Initialize()` 中增加重复加载与版本匹配检测：读取系统变量 `ACADVER`，检查当前程序集 TargetFramework 是否与运行环境匹配；若在低版本误触发高版本 DLL 或检测到同命名空间已注册，主动跳过初始化并记录安全日志。
4. P0 验证项必须覆盖三包隔离与 AutoCAD 2025 双包加载边界场景。

### 6.9 国产 CAD 运行时可用性检测

**浩辰 CAD**：.NET API 使用 `GrxCAD.*` 命名空间对应 AutoCAD 的 `Autodesk.AutoCAD.*`，属于反射式映射而非原生实现。某些 API 可能在接口层面存在但在运行时返回 `E_NOTIMPL`。例如 `ViewTableRecord.SetCurrentView()` 在 GStarCAD 2022 中不生效，FLATSHOT 对话框无法通过 `CMDDIA=0` 抑制。

**中望 CAD**：官方宣称支持 .NET API，但独立第三方技术文档有限。XRecord 的 ExtensionDictionary 行为需实测验证。

**适配层要求**：
- `ICadPlatformAdapter` 定义关键 API 抽象。
- 为浩辰 CAD 的 `GrxCAD.*` 命名空间建立适配映射层，而非简单替换命名空间引用。
- P0 预研必须增加“API 接口存在但运行时未实现”的检测项。
- 预研报告需包含每个关键 API 的“接口层可用性”和“运行时可用性”双维度结论。

### 6.10 AI 语义审查实现

`SemanticSearchService` 负责模型加载、embedding 生成、余弦相似度计算。模型加载失败时降级为关键词检索。

### 6.11 审计哈希链

`AuditService` 负责追加审计、计算链式哈希、校验完整性。`CurrentHash = SHA256(PreviousHash + JSON(AuditEntry without CurrentHash))`。序列化时排除 `CurrentHash` 字段。

### 6.12 版本对比联动

`CompareLinkService` 负责调用 `COMPARE`、捕获比较云线、包围盒相交、建立映射。

### 6.13 PaletteSet 线程

PaletteSet 内嵌 UserControl 时，所有 CAD API 调用必须在主线程。使用 `Application.DocumentManager.MdiActiveDocument` 获取当前文档，通过 `DocumentLock` 写入。文档切换时刷新列表数据。

---

## 7. 版本兼容性

### 7.1 AutoCAD 版本矩阵

| AutoCAD 版本 | 内核系列 (Series) | .NET 运行时/目标框架 | 开发/构建环境 | v0.1.0 支持包 | 核心特性与降级策略 |
| --- | --- | --- | --- | --- | --- |
| 2007-2009 | R17.0 - R17.2 | .NET Framework 3.5 (CLR 2.0) | VS 2019 / VS 2022 | 远古兼容宿主 (Net35) | 无 accoremgd；无原生 WPF（降级 WinForms 对话框与命令行）；无 MLeader（采用经典 Leader+MText 复合结构）；Inno Setup 注册表自动注入自启；无原生 COMPARE（降级提示） |
| 2010-2012 | R18.0 - R18.2 | .NET Framework 3.5 (CLR 2.0) | VS 2019 / VS 2022 | 远古兼容宿主 (Net35) | 无 accoremgd；无原生 WPF（降级 WinForms 对话框与命令行）；Inno Setup 注册表自动注入自启；无原生 COMPARE（降级提示） |
| 2014 | R19.1 | .NET Framework 4.5 | VS 2019 / VS 2022 | 经典兼容包 (Net45) | 无原生 COMPARE（降级提示）；浅色 UI；AI 依赖 .NET Standard 2.0 或降级纯关键词 |
| 2015-2016 | R20.0 - R20.1 | .NET Framework 4.5 | VS 2019 / VS 2022 | 经典兼容包 (Net45) | 无原生 COMPARE（降级提示）；深色 UI；AI 依赖 .NET Standard 2.0 或降级纯关键词 |
| 2017-2018 | R21.0 - R22.0 | .NET Framework 4.6 (向下兼容 4.5) | VS 2019 / VS 2022 | 经典兼容包 (Net45) | 无原生 COMPARE（降级提示）；高分屏适配；AI 依环境全功能或降级 |
| 2019-2020 | R23.0 - R23.1 | .NET Framework 4.7 (向下兼容 4.5) | VS 2019 / VS 2022 | 经典兼容包 (Net45) | 原生支持 COMPARE 版本差异联动；全功能 |
| 2021-2024 | R24.0 - R24.3 | .NET Framework 4.8 | VS 2019 / VS 2022 | 主流基线包 (Net48) | 全功能基线；原生 COMPARE 差异联动；原生 ONNX AI 语义检索 |
| 2025-2026 | R25.0 - R25.1 | .NET 8.0 (`net8.0-windows`) | VS 2022 v17.10+ | 新一代扩展包 (Net8) | 现代化 .NET 8 运行时；Series 严格隔离防双包冲突；全功能 |
| 2027 | R26.0 - R27.0 | .NET 10.0 (`net10.0-windows`) | VS 2022 / .NET 10 SDK | 最新世代包 (Net10) | 现代化 .NET 10 运行时；提前适配 R27 全代宿主 |

> **多版本覆盖策略**：v0.1.0 建立“源码共享 (Core.shproj) + 多宿主外壳 (R17~R27) + 双轨 UI (WPF / WinForms) + 注册表与 Bundle 双轨分发”体系，全面覆盖 AutoCAD 2007-2027，防范 CLR 框架不匹配与 SDK Forward Binding 缺失导致的崩溃风险。

### 7.2 中望、浩辰 CAD 兼容策略

1. 核心业务层不依赖 CAD，新增平台适配层即可。
2. 完成最小原型预研：XRecord、XData、反应器、REVCLOUD、TABLE。
3. TABLE 支持不足时，汇总表降级 `LINE + MTEXT`；导出降级 CSV。
4. 预研报告作为 v0.1.0 交付物。
5. 必须检测“接口存在但运行时未实现”的 API。
6. 浩辰 CAD 使用 `GrxCAD.*` 适配映射层。

### 7.3 兼容性表述

统一为：

> “采用 DWG 原生对象与标准数据机制，尽可能降低跨 CAD 数据丢失风险；具体平台兼容性以 v0.1.0 兼容性验证报告为准。”

禁止使用“完美兼容”“任何 CAD”“100% 兼容”。

---

## 8. 开发计划

### 8.1 阶段划分

| 阶段 | 周期 | 核心任务 | 交付物 |
| --- | --- | --- | --- |
| P0a - AutoCAD 基线与多版本技术验证 | 4 周 | 单批注创建 + XRecord 分片 + XData + REVCLOUD + MTEXT + ESC + UNDO/REDO + COPY + COPYCLIP + 多文档 + 比例 + 插件卸载重载恢复 + 三包隔离与 AutoCAD 2025 双包加载检测 + EXPLODE 保护 + Autoloader `<Commands>` 按需加载验证 + ONNX 非托管加载与 net45 降级验证 | 技术验证报告 + 单批注原型 |
| P0b - 国产 CAD 预研与跨平台验证 | 2-3 周 | 中望、浩辰 XRecord / XData / 反应器 / REVCLOUD / TABLE 运行时可用性检测 + GrxCAD 适配映射 + SeriesMin/SeriesMax 实测 | 国产 CAD 预研报告 + 适配层原型 |
| P1 - 核心稳定性 | 6-8 周 | 反应器延迟队列 + 冲突解决 + Clone 检测 + MigrationService + NOTECHECK + NOTEQUERY + 增删改 + 批注回复 + 审计日志 | 功能完整管理工具 |
| P2 - 业务增强 | 7-9 周 | Export + Summary + KnowledgeBase + NOTEADAPT + 图层显隐 + JSON 导入导出 + 跨图迁移 + 序号前后缀 + 状态可视化 + 天正比例深度适配 | 完整审图工作流 |
| P3 - 拓展与发布 | 8-10 周 | PaletteSet 面板 + 版本对比联动 + AI 语义审查 + 审查包 + 批量多图合并 + 中望浩辰适配 + 多版本 AutoCAD 测试 + 安装器 + 回滚 + 日志完善 + SeriesMin/Max 收口 | v0.1.0 正式发布版 |

**v0.1.0 总周期**：27-34 周。

### 8.2 P0 核心验证项

```text
1. 单条批注创建（PendingNote + 短事务 + 最终确认）
2. XRecord 4+2 分片读写 + 回复/审计分卷
3. XData 多 RegApp 共存
4. REVCLOUD 生成 + 图层非打印 + 新图元捕获
5. MTEXT 生成 + 内容格式
6. 用户 ESC → 草稿回滚，无残留
7. UNDO / REDO 基础校验与精细同步
8. COPY 整批注 → 新 NoteId 重建
9. COPYCLIP 跨图 → 同上
10. 多文档切换 → 状态隔离
11. 比例自适应 → 天正 / AutoCAD 原生
12. 插件卸载 / 重载 → 数据完整恢复
13. 中望、浩辰 XRecord / XData / 反应器 / REVCLOUD / TABLE 运行时可用性
14. INSERT / WBLOCK / BIND / XREF 策略化迁移
15. SeriesMin/SeriesMax 与 Runtime 映射实测（覆盖 R19.1~R25.1 完整系列）
16. 三包隔离与 AutoCAD 2025 双包同时加载检测
17. 延迟队列冲突解决
18. 审计链完整性
19. AI 模型加载与语义检索
20. 版本对比联动（AutoCAD 2019-2026 原生启用；2014-2018 捕获未知命令并优雅降级）
21. PaletteSet 非模态面板
22. 审查包导出/导入
23. 批量多图合并
24. Autoloader 按需加载 <Commands> 声明验证
25. ONNX 非托管 SetDllDirectory 加载验证
26. EXPLODE 不剥离 XRecord 分片验证
27. Net45 经典兼容包在 AutoCAD 2014-2020 运行环境、SDK 兼容性与依赖降级验证
```

### 8.3 质量目标

- Core 层单元测试覆盖率 ≥ 70%。
- 每个 P0 验证项必须有自动化测试或半自动脚本。
- Code Review 检查项：`entity.XData =` 仅出现在 `XDataHelper`。
- 所有 P0 验证项通过后进入 P1。
- 新增功能（审计、AI、审查包、批量合并）必须有集成测试。

### 8.4 预留抽象

`INoteRepository`、`ICloudDrawer`、`IXDataAccessor`、`ICadPlatformAdapter`。

---

## 9. 部署方案

### 9.1 三包结构（Triple Bundle）

```text
CADAutoCloudNote.Net45.bundle/
├── PackageContents.xml
└── Contents/
    ├── CADAutoCloudNote.dll
    └── deps/

CADAutoCloudNote.Net48.bundle/
├── PackageContents.xml
└── Contents/
    ├── CADAutoCloudNote.dll
    └── deps/

CADAutoCloudNote.Net8.bundle/
├── PackageContents.xml
└── Contents/
    ├── CADAutoCloudNote.dll
    └── deps/
```

### 9.2 PackageContents.xml

#### net45 经典兼容包示例（AutoCAD 2014-2020）

```xml
<?xml version="1.0" encoding="utf-8"?>
<ApplicationPackage SchemaVersion="1.0"
    AppVersion="0.1.0"
    Name="CAD Auto CloudNote (Net45)"
    Description="图纸批注与校审协同工具 - AutoCAD 2014-2020"
    Author="Your Name">
    <CompanyDetails Name="Your Company" />
    <RuntimeRequirements OS="Win64"
        Platform="AutoCAD*"
        SeriesMin="R19.1"
        SeriesMax="R23.1" />
    <Components>
        <ComponentEntry AppName="CADAutoCloudNote"
            ModuleName="./Contents/CADAutoCloudNote.dll"
            AppDescription="CloudNote Plugin (net45)"
            LoadOnAutoCADStartup="False">
            <Commands GroupName="CADNOTE">
                <Command Global="CLOUDNOTE" Local="CLOUDNOTE"/>
                <Command Global="NOTEQUERY" Local="NOTEQUERY"/>
                <Command Global="NOTEREPLY" Local="NOTEREPLY"/>
                <Command Global="NOTEOUT" Local="NOTEOUT"/>
                <Command Global="NOTESUMMARY" Local="NOTESUMMARY"/>
                <Command Global="NOTEKB" Local="NOTEKB"/>
                <Command Global="NOTESHOW" Local="NOTESHOW"/>
                <Command Global="NOTESET" Local="NOTESET"/>
                <Command Global="NOTEDEL" Local="NOTEDEL"/>
                <Command Global="NOTEADAPT" Local="NOTEADAPT"/>
                <Command Global="NOTECOMPARE" Local="NOTECOMPARE"/>
                <Command Global="NOTEPANEL" Local="NOTEPANEL"/>
                <Command Global="NOTECOLOR" Local="NOTECOLOR"/>
                <Command Global="NOTEAISEARCH" Local="NOTEAISEARCH"/>
                <Command Global="NOTEAUDIT" Local="NOTEAUDIT"/>
                <Command Global="NOTEPKG" Local="NOTEPKG"/>
                <Command Global="NOTEIMPORT" Local="NOTEIMPORT"/>
                <Command Global="NOTEBATCH" Local="NOTEBATCH"/>
                <Command Global="NOTECHECK" Local="NOTECHECK"/>
                <Command Global="NOTELOG" Local="NOTELOG"/>
            </Commands>
        </ComponentEntry>
    </Components>
</ApplicationPackage>
```

#### net48 基线包示例（AutoCAD 2021-2024）

```xml
<?xml version="1.0" encoding="utf-8"?>
<ApplicationPackage SchemaVersion="1.0"
    AppVersion="0.1.0"
    Name="CAD Auto CloudNote (Net48)"
    Description="图纸批注与校审协同工具 - AutoCAD 2021-2024"
    Author="Your Name">
    <CompanyDetails Name="Your Company" />
    <RuntimeRequirements OS="Win64"
        Platform="AutoCAD*"
        SeriesMin="R24.0"
        SeriesMax="R24.3" />
    <Components>
        <ComponentEntry AppName="CADAutoCloudNote"
            ModuleName="./Contents/CADAutoCloudNote.dll"
            AppDescription="CloudNote Plugin (net48)"
            LoadOnAutoCADStartup="False">
            <Commands GroupName="CADNOTE">
                <Command Global="CLOUDNOTE" Local="CLOUDNOTE"/>
                <Command Global="NOTEQUERY" Local="NOTEQUERY"/>
                <Command Global="NOTEREPLY" Local="NOTEREPLY"/>
                <Command Global="NOTEOUT" Local="NOTEOUT"/>
                <Command Global="NOTESUMMARY" Local="NOTESUMMARY"/>
                <Command Global="NOTEKB" Local="NOTEKB"/>
                <Command Global="NOTESHOW" Local="NOTESHOW"/>
                <Command Global="NOTESET" Local="NOTESET"/>
                <Command Global="NOTEDEL" Local="NOTEDEL"/>
                <Command Global="NOTEADAPT" Local="NOTEADAPT"/>
                <Command Global="NOTECOMPARE" Local="NOTECOMPARE"/>
                <Command Global="NOTEPANEL" Local="NOTEPANEL"/>
                <Command Global="NOTECOLOR" Local="NOTECOLOR"/>
                <Command Global="NOTEAISEARCH" Local="NOTEAISEARCH"/>
                <Command Global="NOTEAUDIT" Local="NOTEAUDIT"/>
                <Command Global="NOTEPKG" Local="NOTEPKG"/>
                <Command Global="NOTEIMPORT" Local="NOTEIMPORT"/>
                <Command Global="NOTEBATCH" Local="NOTEBATCH"/>
                <Command Global="NOTECHECK" Local="NOTECHECK"/>
                <Command Global="NOTELOG" Local="NOTELOG"/>
            </Commands>
        </ComponentEntry>
    </Components>
</ApplicationPackage>
```

#### net8.0 扩展包示例（AutoCAD 2025-2026）

```xml
<?xml version="1.0" encoding="utf-8"?>
<ApplicationPackage SchemaVersion="1.0"
    AppVersion="0.1.0"
    Name="CAD Auto CloudNote (Net8)"
    Description="图纸批注与校审协同工具 - AutoCAD 2025-2026"
    Author="Your Name">
    <CompanyDetails Name="Your Company" />
    <RuntimeRequirements OS="Win64"
        Platform="AutoCAD*"
        SeriesMin="R25.0"
        SeriesMax="R25.1" />
    <Components>
        <ComponentEntry AppName="CADAutoCloudNote"
            ModuleName="./Contents/CADAutoCloudNote.dll"
            AppDescription="CloudNote Plugin (net8.0)"
            LoadOnAutoCADStartup="False">
            <Commands GroupName="CADNOTE">
                <Command Global="CLOUDNOTE" Local="CLOUDNOTE"/>
                <Command Global="NOTEQUERY" Local="NOTEQUERY"/>
                <Command Global="NOTEREPLY" Local="NOTEREPLY"/>
                <Command Global="NOTEOUT" Local="NOTEOUT"/>
                <Command Global="NOTESUMMARY" Local="NOTESUMMARY"/>
                <Command Global="NOTEKB" Local="NOTEKB"/>
                <Command Global="NOTESHOW" Local="NOTESHOW"/>
                <Command Global="NOTESET" Local="NOTESET"/>
                <Command Global="NOTEDEL" Local="NOTEDEL"/>
                <Command Global="NOTEADAPT" Local="NOTEADAPT"/>
                <Command Global="NOTECOMPARE" Local="NOTECOMPARE"/>
                <Command Global="NOTEPANEL" Local="NOTEPANEL"/>
                <Command Global="NOTECOLOR" Local="NOTECOLOR"/>
                <Command Global="NOTEAISEARCH" Local="NOTEAISEARCH"/>
                <Command Global="NOTEAUDIT" Local="NOTEAUDIT"/>
                <Command Global="NOTEPKG" Local="NOTEPKG"/>
                <Command Global="NOTEIMPORT" Local="NOTEIMPORT"/>
                <Command Global="NOTEBATCH" Local="NOTEBATCH"/>
                <Command Global="NOTECHECK" Local="NOTECHECK"/>
                <Command Global="NOTELOG" Local="NOTELOG"/>
            </Commands>
        </ComponentEntry>
    </Components>
</ApplicationPackage>
```

> 说明：三包 SeriesMin 与 SeriesMax 严格无缝拼接（R19.1~R23.1 / R24.0~R24.3 / R25.0~R25.1），确保 Autoloader 精确按 CAD 系列号加载匹配二进制，防范跨系列误加载与类型冲突风险。最终边界以 P0 实测收口为准。后续新增命令必须同步加入三包的 `<Commands>`。

### 9.3 第三方依赖加载

```csharp
using System.Runtime.InteropServices;

[DllImport("kernel32.dll", CharSet = CharSet.Unicode, SetLastError = true)]
private static extern bool SetDllDirectory(string lpPathName);

public void Initialize()
{
    var baseDir = Path.GetDirectoryName(Assembly.GetExecutingAssembly().Location);
    var depsPath = Path.Combine(baseDir, "deps");

    // 必须先注入非托管 DLL 搜索路径，供 onnxruntime.dll 等原生库使用
    if (Directory.Exists(depsPath))
    {
        SetDllDirectory(depsPath);
    }

    // AssemblyResolve 只负责托管程序集
    AppDomain.CurrentDomain.AssemblyResolve += (sender, args) =>
    {
        var asmName = new AssemblyName(args.Name).Name + ".dll";
        var fullPath = Path.Combine(depsPath, asmName);
        return File.Exists(fullPath) ? Assembly.LoadFrom(fullPath) : null;
    };
}
```

### 9.4 双轨制部署体系（Inno Setup 安装器 + Autoloader 应用包）

兼顾标准安装向导与绿色免安装需求：

#### 9.4.1 轨道一：Inno Setup 一键安装器 (`installer/CadAutoCloudNote_Installer.iss`)
- **自动进程守护**：安装与卸载前自动检测 `acad.exe` / `accoreconsole.exe` 进程，友好提示或自动安全中止进程，防止 DLL 占用导致写冲突；
- **全版本注册表智能嗅探**：扫描当前 Windows 系统 `HKEY_CURRENT_USER\Software\Autodesk\AutoCAD` 注册表，自动发现所有已安装的 AutoCAD 版本（覆盖 R19 至 R26 各年份与多语言版本，支持垂直产品如 Architecture、Civil 3D 等）；
- **静默按需/启动加载配置**：
  - 自动向当前激活 Profile 写入 `ACAD` 支持搜索路径；
  - 自动写入 `TRUSTEDPATHS` 受信任路径，杜绝 AutoCAD 2014+ 安全机制弹出的未签名警告；
  - 自动在 Profile 的 `Applications\CadAutoCloudNote` 键下注册 `LOADCTRLS`（按需或启动）、`LOADER`（指向对应版本的 `CadAutoCloudNote_Rxx.dll`）、`MANAGED=1`；
- **交付组件配套**：生成开始菜单项、桌面快捷方式、图文使用手册 `Readme.html`，并在控制面板提供完整反安装卸载项。

#### 9.4.2 轨道二：Autodesk Autoloader 绿色即插即用应用包 (`.bundle`)
- 适用于无管理员权限、内网集中分发或即插即用测试环境；
- 遵循三包架构，放入 `%AppData%\Autodesk\ApplicationPlugins\` 即可由 AutoCAD 原生引擎自动识别与按需加载。

### 9.5 部署与回滚

- 部署方式：优先推荐 Inno Setup 一键安装包；离线免安装使用 `.bundle` 复制到 `%AppData%\Autodesk\ApplicationPlugins\`。
- 回滚：保留旧版本安装包或 bundle，异常时一键覆盖还原。
- 数据修复：`NOTECHECK` 独立运行于任何版本。
- Schema 迁移失败：保留原始 XRecord，不覆盖。
- 多包/双包冲突：`Initialize()` 检测 TargetFramework 与系统变量 `ACADVER`，不匹配跳过。

---

## 10. 版本规划

> v0.1.0 为唯一交付版本。

| 版本 | 规划内容 |
| --- | --- |
| **v0.1.0** | 基础框架；新建批注（比例自适应、普通/手绘云线）；XRecord 4+2 分片读写与回复/审计分卷；批注查询、定位高亮；批注回复；本地知识库词库；AI 语义审查（含 net45 降级保护）；图纸内汇总表；增补删减云线；Excel/CSV 导出；JSON 格式批注导入导出与跨图迁移；序号前缀后缀配置；多文档批注合并；Undo/Redo 精细同步；中望/浩辰 CAD 平台适配；批量多图纸导出批注；UCS 旋转适配；云线编组；INSERT/WBLOCK/BIND/XREF 策略化迁移；SeriesMin/SeriesMax (R19.1~R25.1) 实测收口；Net45/Net48/Net8 三包独立分发；PaletteSet 非模态管理面板；版本对比联动（2019-2026 原生启用，2014-2018 优雅降级）；状态可视化；审计日志；审查包导出/导入；批量多图合并与差异分析；天正比例深度适配；AutoCAD 2025 双包与多版本加载检测；国产 CAD 运行时可用性检测；性能基准达标。 |

**v0.1.0 明确非目标：** 网络飞传、云端协同、账号体系、服务端同步、AutoCAD 2013 及更早版本（不在本支持矩阵内）。

---

## 11. 附录：修复日志、关闭项与编码规范

### 11.1 P0 修复清单

| 编号 | 问题 | 修复方式 | 状态 |
| --- | --- | --- | --- |
| P0-1 | `_CLONE_MARK` 逻辑矛盾 | 统一候选队列 + CommandEnded 批处理 | 已完成 |
| P0-2 | CLOUDNOTE 事务边界错误 | PendingNote + 短事务 + 最终确认 + ESC 回滚 | 已完成 |
| P0-3 | `entity.XData = rb` 覆盖风险 | XDataHelper 唯一入口 | 已完成 |
| P0-4 | 兼容性表述过度 | 收敛为以验证报告为准 | 已完成 |
| P0-5 | SDK 引用方式未验证 | 列为 P0 必做验证项 | 已完成 |
| P0-6 | 部署包与版本矩阵未对齐 | Net45 + Net48 + Net8 三包架构，SeriesMax 实测 | 已完成 |

### 11.2 P1 修复清单

| 编号 | 问题 | 修复方式 | 状态 |
| --- | --- | --- | --- |
| P1-1 | MTEXT 修改来源未建模 | 引入 `ModificationOrigin` | 已完成 |
| P1-2 | `_isCreatingNote` 全局 bool 不安全 | `Dictionary<Database, DocumentState>` | 已完成 |
| P1-3 | `UnregisterAll` 粗暴 | 按 Database 粒度，Register 幂等 | 已完成 |
| P1-4 | Undo/Redo 处理笼统 | 基础 + 精细校验 + NOTECHECK 兜底 | 已完成 |
| P1-5 | 跨图操作未定义 | 补充操作矩阵表 | 已完成 |
| P1-6 | `LinkedEntities` 泛型集合 | 拆为 `Clouds` + `Text` | 已完成 |
| P1-7 | XRecord 16KB 上限 | 拆 4+2 分片 + 回复/审计分卷 | 已完成 |
| P1-8 | 缺 DrawingInstanceId | 增加 `_DRAWING_ID` | 已完成 |
| P1-9 | 部署 SeriesMin/Max 未对齐 | 三包架构严格划分区间（R19.1~R23.1 / R24.0~R24.3 / R25.0~R25.1），实测收口 | 已完成 |
| P1-10 | P0 未纳入 ESC/UNDO/COPY | P0 重排为技术风险验证 | 已完成 |

### 11.3 P2 修复清单

| 编号 | 问题 | 修复方式 | 状态 |
| --- | --- | --- | --- |
| P2-1 | `DrawingInstanceId` 存储位置 | 存入 `_DRAWING_ID` | 已完成 |
| P2-2 | `NOTEADAPT` 实现不明 | 定义 3 种策略 + 天正深度适配 | 已完成 |
| P2-3 | 开发计划风险错位 | P0 拆 P0a/P0b，周期调整 | 已完成 |
| P2-4 | 中望、浩辰预研交付物缺失 | 列为 P0 交付物，增加运行时检测 | 已完成 |

### 11.4 审核修复项

| 编号 | 问题 | 修复方式 | 状态 |
| --- | --- | --- | --- |
| R-1 | 克隆检测方案矛盾 | 统一候选队列 + CommandEnded 批处理 | 已完成 |
| R-2 | 克隆重建一致性缺陷 | 明确整条复制与部分复制判定 | 已完成 |
| R-3 | CLOUDNOTE 短事务边界不清 | 短事务只写图元属性，补充新图元捕获 | 已完成 |
| R-4 | XDataHelper 未注册 RegApp | 增加 EnsureRegApp | 已完成 |
| R-5 | 版本规划多版本 | 全部并入 v0.1.0，仅保留唯一版本 | 已完成 |
| R-6 | SeriesMax 存疑 | 改为实测写死 | 已完成 |
| R-7 | NOTEQUERY 自动清理副作用 | 改为只读展示，清理交 NOTECHECK | 已完成 |
| R-8 | 删减云线顺序风险 | 同一事务先删图元再更新分片 | 已完成 |
| R-9 | 回复状态机过简 | 完善为 Open/Replied/Resolved/Closed | 已完成 |
| R-10 | 知识库路径跨机失效 | 降级提示，不影响批注读取 | 已完成 |
| R-11 | 汇总表强制非打印 | 默认非打印，可配置 | 已完成 |
| R-12 | 多文档 Register 幂等性 | 明确 Register 幂等 | 已完成 |
| R-13 | 反应器事务根因未说明 | 补充 ObjectARX 通知锁与重入计数说明 | 已完成 |
| R-14 | 延迟队列冲突未定义 | 增加 Priority + SequenceNumber + 冲突矩阵 | 已完成 |
| R-15 | XRecord 容量边界未量化 | 硬 32KB / 软 16KB / 每卷 50 条 | 已完成 |
| R-16 | 国产 CAD 运行时风险被低估 | 增加接口/运行时双维度检测 + GrxCAD 适配层 | 已完成 |
| R-17 | AutoCAD 2025 双包与低版本加载未预案 | 三包 Series 严格隔离 + LoadOnAutoCADStartup=False + Initialize 动态判断 | 已完成 |
| R-18 | 缺性能基准 | 定义 7 项量化目标 | 已完成 |
| R-19 | P0 排期过紧 | 拆 P0a/P0b，总周期 27-34 周 | 已完成 |
| R-20 | 拓展未纳入 | 全部纳入 v0.1.0 | 已完成 |

### 11.5 第三版审核修复记录

| 编号 | 问题 | 修复方式 | 状态 |
| --- | --- | --- | --- |
| V3-1 | COMPARE 被误写为 AutoCAD 2014+ | 改为 AutoCAD 2019+（2019-2026 原生启用；2014-2018 优雅降级提示） | 已完成 |
| V3-2 | AutoCAD 2027 目标与非目标冲突 | 全文档收敛为 2021-2024 + 2025-2026，移除 2027 | 已完成 |
| V3-3 | net48 宣称覆盖 AutoCAD 2014 | 设立专用 Net45 经典兼容包覆盖 AutoCAD 2014-2020，消除单程序集跨大版本覆盖的弊端 | 已完成 |
| V3-4 | PaletteSet 宿主方向颠倒 | 改为 `ElementHost`，禁用 `WindowsFormsHost` | 已完成 |
| V3-5 | ONNX 非托管 DLL 无法被 AssemblyResolve 拦截 | 增加 `SetDllDirectory(depsPath)` | 已完成 |
| V3-6 | EXPLODE 误触发 EraseTask 导致业务绑定丢失 | 增加 EXPLODE 命令过滤与 `ExplodeEraseCandidate` | 已完成 |
| V3-7 | Autoloader 按需加载死锁 | 补全 `<Commands GroupName="CADNOTE">` | 已完成 |

### 11.6 第四版支持矩阵修复记录（AutoCAD 2014-2020 纳入）

| 编号 | 问题 | 修复方式 | 状态 |
| --- | --- | --- | --- |
| V4-1 | AutoCAD 2014-2020 排除在支持矩阵外 | 建立 Net45 经典兼容包，升级构建“Net45 + Net48 + Net8”三包独立打包架构 | 已完成 |
| V4-2 | Autoloader 缺少 2014-2020 系列定义 | 新增 Net45 PackageContents.xml，定义 SeriesMin="R19.1"、SeriesMax="R23.1" | 已完成 |
| V4-3 | 项目配置单一 net48 导致低版本无法编译 | 升级为 `<TargetFrameworks>net45;net48;net8.0-windows</TargetFrameworks>` 多目标编译 | 已完成 |
| V4-4 | NOTECOMPARE 在 2019-2020 被误排除 | 调整为 2019-2026 全面启用，2014-2018 优雅降级提示 | 已完成 |
| V4-5 | Net45 环境下 ONNX 依赖可能缺失 | 增加 AI 语义检索在缺失 .NET Standard 2.0 时的纯 C# 关键词降级保障 | 已完成 |

### 11.7 第五版核心更新记录（多边形云线、复合图元级联闭环、4 大紧凑板块与高级联动）

| 编号 | 问题 | 修复方式 | 状态 |
| --- | --- | --- | --- |
| V5-1 | 多边形云线凹凸不分、圆弧倒翻刺入内部 | 引入 `PolygonCloudJig` 并应用 Shoelace（鞋带定理）面积算法自适应判定绕向，动态反转 Bulge 凸度 | 已完成 |
| V5-2 | 仅删除引线或三角形端头导致批注死锁与孤立遗留 | 在 `ReactorManager` 建立复合图元弱引用索引，删除引线/端头触发级联闭环安全删除 | 已完成 |
| V5-3 | 夹点拖拽编辑移动文字时误触端头及画面残影 | 夹点变换状态下利用 `Application.Idle` 空闲泵统一刷新引线与重绘，消除滞后与残影 | 已完成 |
| V5-4 | 配置选项过于分散且功能单一 | 重构整合为 4 大紧凑业务大板块，并搭载严重等级颜色映射、审查词典、健康体检 3 大高级联动 | 已完成 |
| V5-5 | AutoCAD 2027 (.NET 10.0) 未来宿主缺乏独立适配 | 新增 `CadAutoCloudNote.R27` 独立宿主工程与 `Libs/AutoCAD/Net10.0_2027` 引用，全系统 11 代宿主独立隔离编译 | 已完成 |
| V5-6 | 快速审图频繁弹出输入框阻碍节奏 | 新增极速批注模式（`CNFAST` / `CNS`），一键回车默认以“待修改”落图，看板统一后置微调 | 已完成 |

### 11.8 v0.1.0 关闭项

1. **Undo/Redo 精细同步**：监听 `ModifyUndone` / `Reappended` / `Unappended` + CommandEnded 校验 + NOTECHECK 兜底。  
2. **跨图纸操作矩阵完整实现**：COPYCLIP / INSERT / WBLOCK / BIND / XREF 策略化迁移。  
3. **XRecord 分片扩展**：回复分片与审计分片自动分卷。  
4. **SeriesMin/SeriesMax 与 Runtime 映射**：覆盖 R19.1~R25.1 完整三包系列，P0 实测后写死。  
5. **网络飞传、云端协同**：明确非目标，不纳入任何版本。  
6. **全部拓展实现**：版本对比联动、PaletteSet 面板、状态可视化、AI 语义审查、审计日志、审查包、批量多图合并、天正比例深度适配。  

### 11.9 编码规范强制条款

1. 全项目搜索 `entity.XData =` 只允许出现在 `XDataHelper.cs`。  
2. 反应器回调内禁止开启任何 CAD 事务。  
3. 所有写 DWG 必须包裹 `DocumentLock`。  
4. 反应器状态必须按 `Database` 隔离，禁止全局单例。  
5. 长交互命令禁止持有长期事务，必须 PendingDraft + 短事务。  
6. XRecord 写入必须按主体/云线/文字/回复/审计分片形式，禁止单条巨型 JSON；回复与审计分片必须支持分卷。  
7. 写入 XData 前必须确保 `CAD_CLOUD_NOTE_LINK` RegApp 已注册。  
8. NOTEQUERY 等查询命令不得静默写回数据，修复统一由 NOTECHECK 执行。  
9. 克隆部分复制不得静默清除 XData，必须标记并交由 NOTECHECK 或用户确认。  
10. 所有版本号、交付物、计划统一使用 v0.1.0，禁止出现其他版本实现规划。  
11. 审计写入必须与主体写操作同一事务，链式哈希不可跳过。  
12. AI 模型加载失败必须降级为关键词检索，不得阻塞批注主流程。  
13. 审查包导入必须校验包哈希，坐标映射必须支持 UCS 校准。  
14. 批量多图必须使用只读副本，禁止修改原图。  
15. 非托管 DLL 加载：`SetDllDirectory(depsPath)` 必须在 `Initialize()` 最前置调用；`AssemblyResolve` 只用于托管程序集。  
16. `PackageContents.xml` 若设置 `LoadOnAutoCADStartup="False"`，必须声明完整 `<Commands>` 节点，否则插件无法按需加载。  
17. `EXPLODE` 活动期间不得自动剥离 XRecord 分片 Handle，必须标记 `OrphanCloneCandidate` 并交 `NOTECHECK` 或用户确认。  
18. 版本文档与部署包统一规范为“AutoCAD 2014-2020 经典兼容包（Net45） + AutoCAD 2021-2024 主流基线包（Net48） + AutoCAD 2025-2026 新一代扩展包（Net8）”三包架构，Autoloader 系列号无缝拼接且严格不重叠（R19.1~R23.1 / R24.0~R24.3 / R25.0~R25.1），禁止文档内出现冲突描述。  
19. 核心业务与领域逻辑必须全部在 `CadAutoCloudNote.Core` 共享工程中开发，宿主外壳工程（`src/CadAutoCloudNote.Rxx`）严禁编写业务代码，保持纯粹宿主外壳。  
20. **表情图标与符号禁令（红线条款）**：非特殊情况，全项目（包含但不限于 UI 控件文本、命令行提示与回显信息、日志输出、技术文档、白皮书、注释及交流文本）严禁使用任何表情图标（Emoji）。关于何为“特殊情况”，在此明确界定：仅且仅在用户明确下达书面指示要求使用、或外部行业标准/第三方数据包字段有不可变更的硬性符号要求时，方可构成特殊情况；在任何日常开发、界面呈现、方案阐述与说明中，一律不存在特殊情况，必须严格执行纯文本与工程级规范呈现。  
21. **保守务实与反夸大准则（红线条款）**：所有的功能实现、技术描述、架构方案、性能指标与兼容性结论必须始终保持保守、客观、严谨、务实，坚决不能夸大其词，拒绝使用任何绝对化、100%肯定的话语（例如严禁使用“完美兼容”、“绝对安全”、“彻底根除”、“毫无悬念”、“100%杜绝”等词汇）。所有工程实现均须基于明确的防御性编码、异常捕获机制、边界检验与可降级策略进行客观严谨表述。  
22. **全版本宿主支持列表（AutoCAD 2007~2027）**：基于“共享核心工程 + 11 代独立宿主外壳（R17~R27）+ Inno Setup 单文件安装包”体系落地，涵盖 AutoCAD 2007~2012（.NET 3.5 / WinForms 降级）、AutoCAD 2013~2024（.NET 4.8 / WPF）、AutoCAD 2025~2026（.NET 8.0 / WPF）及 AutoCAD 2027（.NET 10.0-windows / WPF），各宿主严格隔离编译并保持独立性。  

---

**文档结束**